# UI-NOTES ― レイアウト/UX 改善メモ

機能実装と並行して育てる UI の気づきを記録。各STEPや仕上げ（STEP8）で拾う。
詳細仕様は [spec.md](./spec.md)、進行は [ROADMAP.md](./ROADMAP.md)。

| # | 内容 | 反映タイミング（目安） | 状態 |
|---|---|---|---|
| 1 | **ホームモードの妖精をもっと大きく表示**。現状 `lg`=176px は小さめ。中央でもっと存在感を出すイメージ（画面高さの半分前後など） | ホーム画面を作り込むSTEP（会話UI実装時）／遅くともSTEP8 | ⬜ |
| 2 | **下部の「ホーム / カメラ」は常時2つ表示ではなく切り替え型に**。両タブ常設ではなく、モードを切り替えるトグル/ボタン的なナビにする想定 | ナビ設計を見直すSTEP／STEP8 仕上げ | ⬜ |
| 3 | **会話UIの作り込み**（STEP2で最小機能を実装済）。妖精＋会話の縦積みレイアウト・吹き出しの見た目・妖精サイズ(#1)との兼ね合い・空状態/送信中の演出・履歴が伸びた時のスクロール挙動などを整える。表情差分（thinking/happy/sad）は実画像が無いと neutral にフォールバックするので、差分イラストが入ったら確認 | 会話UIを作り込むSTEP／STEP8 仕上げ | ⬜ |
| 4 | **漫符（STEP11A）の出る位置・大きさを実機で詰める**。`Sprite2DRenderer` で妖精ラッパ右上（`-right-1 -top-1`）に `animate-manpu` で表示。特に `sm`（カメラ右下＝画面端に近い）で漫符が画面外にはみ出さないか・小さすぎないか、`lg`（ホーム中央）で位置と大きさが自然か、1.2s の出方が速/遅すぎないかを目視。色（怒=赤/汗=水色/きらめき=黄/！=ピンク/?=青）がパレットから浮かないかも確認 | STEP11A 実機目視／その後の漫画演出STEP | ⬜ |

## メモ
- 現状の各モード画面はプレースホルダー。レイアウトは機能実装に合わせて整える方針。
- カラー/フォントのデザイントークンは `tailwind.config.js` に定義済み（追記はそこへ）。
- 妖精サイズは `src/lib/character/Sprite2DRenderer.tsx` の `SIZE_CLASS`、ナビは `src/components/ModeSwitcher.tsx`。

## v2 UX メモ（2026-07-02・要実機で詰める）
| # | 内容 | 反映タイミング | 状態 |
|---|---|---|---|
| v2-1 | **妖精界シーンのレイアウト**：世界観として作り込んだ背景＋アイテム配置レイヤー。**コレットが嬉しそうに置く演出**→後でユーザーが移動可（ドラッグ）。アイテムは~1日1個ずつ増える＝"ガランとしない・でも育つ"の見せ方。透過アイテムが背景から浮かないか（`ART_STYLE_BLOCK` 一貫性） | STEP1（暫定）→ STEP5 で仕上げ | ⬜ |
| v2-2 | **アルバムのレイアウト**：保存写真のグリッド/タイムライン。**コレットが写真に言及**する導線（タップで「これ、あの日撮ったね」）。「1ヶ月前の今日」など思い出の浮上。窯への動線（この写真をアイテム化） | STEP1→STEP2 | ⬜ |
| v2-3 | **オンボ/チュートリアル**：はじめまして＝コレットの人柄提示＋操作学習＋**初期シード**（初写真→初アイテム→妖精界に初出現）。過多で離脱↔不足で混乱のバランス。終了時点で"空っぽ"にしない | STEP4 | ⬜ |
| v2-4 | **声のUX**：カメラ＝動的TTS の**悪通信フォールバック**（テキスト先行→音声後追い・楽観UI・"電波さがすね…"）。ホーム＝パートボイスの"間"・字幕表示・ON/OFF の置き場 | STEP3 | ⬜ |
| v2-5 | **ゲージの見せ方**：「コレットの元気/絆」として。**通貨/ノルマに見せない**（streak 不安を出さない）。満タン→「今日の1個」アイテム化の解禁演出 | STEP1→STEP4 | ⬜ |
| v2-6 | **アイテム化の演出**：1日1個の**儀式感**（希少で貴重）。窯→**妖精界に出現する瞬間**を見せ場に。コレットの特大リアクション | STEP1→STEP5 | 🚧 STEP1＝窯で透過reveal＋大喜び。妖精界に飛ぶ導線あり。「出現する瞬間」の演出強化はSTEP5 |
| v2-7 | **ホーム5タブの窮屈さ**：おしゃべり/ずかん/アルバム/妖精の窯/妖精界の5タブは狭い画面で詰まる（現状 text-xs＋横スクロールで凌ぐ）。将来ナビ再設計（#2）と併せて整理（例：ずかん↔アルバムのまとめ、窯を妖精界に内包 等） | ナビ再設計STEP／STEP5 | ⬜ |
| v2-8 | **妖精界の配置UX**：STEP1 は擬似ランダム自動配置＋自由ドラッグ（背景プレースホルダ）。本番は**コレットが嬉しそうに置くアニメ**・重なり回避・地面/棚などアンカー・透過アイテムが背景から浮かないか（`ART_STYLE_BLOCK` 一貫性）を実機で詰める | STEP5 | ⬜ |

- ※旧 `codex/` は `album/` に置換。妖精サイズ #1（ホーム中央で大きく）は v2 でも有効（会話・妖精界で存在感）。
- ※v2-1/v2-5/v2-6 は STEP1（1e/1f）で暫定実装済＝仕上げは STEP5／マネタイズ STEP。
