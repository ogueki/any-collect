# UI-NOTES ― レイアウト/UX の宿題

**まだ決着していない UI の気づきだけを置く。** 決着した知見（画面高の吸収・雲＋ハロー・`drop-shadow` を光に使わない・リロード後の見え方 等）は [DECISIONS.md](./DECISIONS.md) に移設済み。仕様＝[spec.md](./spec.md)、進行＝[ROADMAP.md](./ROADMAP.md)。

**状態**：**⬜ 未着手**／**🚧 一部対応（残を明記）**

| # | 内容 | 状態 |
|---|---|---|
| 1 | **ホームのコレットをもっと大きく**。現状 `lg`=176px は小さめ。中央でもっと存在感を出す（画面高さの半分前後など） | ⬜ |
| 5 | **ホーム背景（木のうろの部屋×時間帯4枚）の実機目視**：①白カード/吹き出しの可読性 ②立ち絵と家具の重なり ③縦長端末で左右 ~15% クロップ ④時間帯切替の妥当性 | 🚧 朝は確認済（①〜③OK・丸窓がステータスバー裏に隠れ気味は許容）。**残＝夕/夜の見え方・半透過後の可読性再確認** |
| v2-1 | **たからばこの実機での手触り**：「浮いてる感」／暗い背景と透過アイテムの相性（`ART_STYLE_BLOCK` 一貫性）／掴んだときのスナップの気持ちよさ | 🚧 構造は headless 検証済（星の分布・漂いの位相/周期・奥行き）。**残＝実機** |
| v2-8 | **たからばこの漂いのチューニング**：速さ（`DRIFT_MIN_S`/`DRIFT_MAX_S`＝現 6〜11s）・振れ幅（`drift` keyframe の translate 量）・掴んだ瞬間のスナップ量 | 🚧 **残＝実機** |
| v2-2 | **アルバムのレイアウト**：グリッド/タイムライン。**コレットが写真に言及**する導線（タップで「これ、あの日撮ったね」）。「1ヶ月前の今日」など思い出の浮上（※アイテム化への動線は図鑑＝召喚に統一済みなのでアルバムからは張らない） | ⬜ |
| v2-4 | **声のUX**：カメラ＝動的TTS の悪通信フォールバック（テキスト先行→音声後追い・楽観UI）。ホーム＝パートボイスの"間"・字幕表示・ON/OFF の置き場 | ⬜（3b とセット） |
| v2-5 | **まほうパワー/なつきの見せ方**：**通貨/ノルマに見せない**（streak 不安を出さない）。満タン→召喚の解禁演出 | 🚧 撮影時の＋ポップアップ・満タン演出は実装済。**残＝解禁の儀式感をさらに磨く**。なつきは上限撤廃に伴い「Lv.N＋次のレベルまでの細いバー」（上限のある表示は天井到達時に「もう何も起きない」ことを毎回伝えてしまう） |
| v2-6 | **召喚の演出**：1日1個の**儀式感**（希少で貴重）。召喚→**たからばこに入る瞬間**を見せ場に。コレットの特大リアクション | 🚧 透過 reveal＋大喜びは実装済。**残＝「出現する瞬間」の演出強化** |
| 図鑑 | **標本の台紙の実機目視（作り直し後）**：紙の濃さ（`paper #F3E9D2`）／ノイズ強度（0.16・140px タイル）／明朝の可読性（Shippori Mincho・小さい字がつぶれないか）／白フチと傾きの見え方／空きマスの濃さ／アルバムとの対比 | 🚧 **残＝実機**。PC のモックでは紙に見えることを確認済み、実機の見え方はこれから |
| 失敗 | **失敗時のコレットのセリフの実機目視**（機内モードで確認） | 🚧 **残＝実機**（08-04 実装ぶん） |

## いまの画面の作り（参照用）
- **ナビ**＝左上のモード切替＋入口ドック（`App.tsx`・`appStore.screen`）。作業画面の右下コレットは `WorkingScreen` 共通シェル。
- **ホームは1画面固定**＝スクロールしない。会話ログ＋記憶パネルは**ボトムシート**（`MenuSheet` 同型）。吹き出し/ステータスバーは半透過＋blur。大セリフは `max-h-40`＋内部スクロールで高さ上限、しっぽなし。
- **大セリフの可読性＝「雲＋ハロー」の二段構え**（`HERO_CLOUD`＋`HERO_TEXT_HALO`）。**片方だけにしない**（理由は DECISIONS）。
- **UI 全体は画面高で一括ズーム**（`html { font-size: clamp(16px, calc(100dvh/46), 20px) }`）。**px 固定の寸法を足すと端末ごとに比率が崩れる**。
- **コレットのサイズ**＝`Sprite2DRenderer` の `SIZE_CLASS`（lg=ホーム中央176px／sm=右下144px）。
- **デザイントークン**（色/フォント）は `tailwind.config.js`。追記もそこへ。
- **検証用ツールは `?debug=1`**（既定オフ・見た目は変わらない・`?debug=0` で解除）＝なつきチップのタップでLv循環／まほうパワーバーのタップで満タン／記憶の「いま覚えて」／会話ログの「1日前にする」／「初期化（全部消す）」。
