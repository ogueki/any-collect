# UI-NOTES ― レイアウト/UX 改善メモ

機能実装と並行して育てる UI の気づきを記録。各STEPや仕上げ（STEP8）で拾う。
詳細仕様は [spec.md](./spec.md)、進行は [ROADMAP.md](./ROADMAP.md)。

| # | 内容 | 反映タイミング（目安） | 状態 |
|---|---|---|---|
| 1 | **ホームモードの妖精をもっと大きく表示**。現状 `lg`=176px は小さめ。中央でもっと存在感を出すイメージ（画面高さの半分前後など） | ホーム画面を作り込むSTEP（会話UI実装時）／遅くともSTEP8 | ⬜ |
| 2 | **下部の「ホーム / カメラ」は常時2つ表示ではなく切り替え型に**。両タブ常設ではなく、モードを切り替えるトグル/ボタン的なナビにする想定 | — | ✅ 決着（2026-07-12 レイアウト再構成＝左上のモード切替に） |
| 3 | **会話UIの作り込み**（STEP2で最小機能を実装済）。妖精＋会話の縦積みレイアウト・吹き出しの見た目・妖精サイズ(#1)との兼ね合い・空状態/送信中の演出・履歴が伸びた時のスクロール挙動などを整える | — | ✅ ほぼ決着（2026-07-13 ホームリデザイン＝大セリフ・入力主役 → 2026-07-19 実機FBで**1画面固定化**＝スクロール廃止・会話ログ＋記憶パネルはボトムシート（MenuSheet同型）へ・吹き出し/ステータスバー半透過＋blur・dvh/overscroll対策 → 大セリフは**縁が溶けるスクリム**＝枠なし見え（radial マスク＋blur・非対応環境は角丸カードにフォールバック・しっぽ廃止）） |
| 4 | **漫符（STEP11A）の出る位置・大きさを実機で詰める**。`Sprite2DRenderer` で妖精ラッパ右上（`-right-1 -top-1`）に `animate-manpu` で表示。特に `sm`（カメラ右下＝画面端に近い）で漫符が画面外にはみ出さないか・小さすぎないか、`lg`（ホーム中央）で位置と大きさが自然か、1.2s の出方が速/遅すぎないかを目視。色（怒=赤/汗=水色/きらめき=黄/！=ピンク/?=青）がパレットから浮かないかも確認 | STEP11A 実機目視／その後の漫画演出STEP | ⬜ |
| 5 | **ホーム背景(木のうろの部屋×時間帯4枚)の実機目視**：①白カード/吹き出しの可読性 ②立ち絵と家具の重なり ③縦長端末で左右 ~15% クロップの見え方 ④時間帯切替（朝/昼/夕/夜・深夜=夜）の妥当性 | 背景設置後の実機確認（push→Vercel） | 🚧 朝は実機確認済（2026-07-19・①〜③OK・丸窓がステータスバー裏に隠れ気味は許容）。実機FB＝背景の見える面積を増やす→吹き出し半透過＋1画面固定で対応。**残＝夕/夜の実機の見え方・半透過後の可読性再確認** |

## メモ
- **ホームの端末高さ吸収（2026-07-19 決着・2段構え）**：viewport 高が可変（Safari アドレスバー有無・端末差・standalone）でも見た目を安定させる。試行錯誤の結論＝**余白の総量は決まっていて"どこに置くか"の問題**。全部上（ステータス↔セリフ）に寄せても全部下（立ち絵↔ボタン）に寄せても、片寄せの帯は目立って嫌われる。
  1. **UI 全体を画面高で一括ズーム**：`index.css` の `html { font-size: clamp(16px, calc(100dvh/46), 20px) }`。Tailwind が rem 基準なので**ルート font-size を上げるだけで全要素が同比率で拡大**（引き伸ばしでなく拡大＝歪まない）→余白の総量そのものを縮める。**調整ノブ＝除数46（16pxになる高さ基準）／上限20px（最大ズーム）**。下限16px＝短い画面の現状維持＋iOS input(16px 未満で自動ズーム)回避。※グローバルだが作業画面はスクロールなので副作用は軽微。
  2. **残った余りは上下"均等"に割る**：ヒーロー（大セリフ＋立ち絵）を `flex-1 justify-center` で HUD とボタンの間に中央寄せ＝片側に帯を作らず、上に薄く・下に薄く分散。下の薄い余白は「床＝部屋の奥行き」として自然に読める。大セリフは `max-h-40`＋内部スクロールで高さ上限。ボタン・入力は最下段固定（親指リーチ）。
  - 参考＝キャラ系アプリ（Pokémon GO・たまごっち系・ガチャホーム）は「キャラを大きく＋操作を下端固定＋余りは景色で吸収」。
- カラー/フォントのデザイントークンは `tailwind.config.js` に定義済み（追記はそこへ）。
- 妖精サイズは `src/lib/character/Sprite2DRenderer.tsx` の `SIZE_CLASS`（lg=ホーム中央176px／sm=右下144px）。
- ナビ＝左上のモード切替＋入口ドック（`App.tsx`・`appStore.screen`）。作業画面の右下コレットは `WorkingScreen` 共通シェル（旧 `ModeSwitcher.tsx` は廃止）。

## v2 UX メモ（2026-07-02・要実機で詰める）
| # | 内容 | 反映タイミング | 状態 |
|---|---|---|---|
| v2-1 | **妖精界シーンのレイアウト**：世界観として作り込んだ背景＋アイテム配置レイヤー。**コレットが嬉しそうに置く演出**→後でユーザーが移動可（ドラッグ）。アイテムは~1日1個ずつ増える＝"ガランとしない・でも育つ"の見せ方。透過アイテムが背景から浮かないか（`ART_STYLE_BLOCK` 一貫性） | STEP1（暫定）→ STEP5 で仕上げ | ⬜ |
| v2-2 | **アルバムのレイアウト**：保存写真のグリッド/タイムライン。**コレットが写真に言及**する導線（タップで「これ、あの日撮ったね」）。「1ヶ月前の今日」など思い出の浮上（※アイテム化への動線は図鑑＝召喚側に統一済みのためアルバムからは張らない） | STEP1→STEP2 | ⬜ |
| v2-3 | **オンボ/チュートリアル**：はじめまして＝コレットの人柄提示＋操作学習＋**初期シード**（初写真→初アイテム→妖精界に初出現）。過多で離脱↔不足で混乱のバランス。終了時点で"空っぽ"にしない | STEP4 | ⬜ |
| v2-4 | **声のUX**：カメラ＝動的TTS の**悪通信フォールバック**（テキスト先行→音声後追い・楽観UI・"電波さがすね…"）。ホーム＝パートボイスの"間"・字幕表示・ON/OFF の置き場 | STEP3 | ⬜ |
| v2-5 | **ゲージの見せ方**：「まほうパワー/なつき」として（旧「元気/絆」から改称済み）。**通貨/ノルマに見せない**（streak 不安を出さない）。満タン→召喚の解禁演出 | STEP1→STEP4 | 🚧 撮影時の＋ポップアップ・満タン演出は実装済（再構成④c）。解禁の儀式感はさらに磨ける |
| v2-6 | **アイテム化の演出**：1日1個の**儀式感**（希少で貴重）。窯→**妖精界に出現する瞬間**を見せ場に。コレットの特大リアクション | STEP1→STEP5 | 🚧 STEP1＝窯で透過reveal＋大喜び。妖精界に飛ぶ導線あり。「出現する瞬間」の演出強化はSTEP5 |
| v2-7 | **ホーム5タブの窮屈さ**：おしゃべり/ずかん/アルバム/妖精の窯/妖精界の5タブは狭い画面で詰まる | — | ✅ 決着（2026-07-12 レイアウト再構成で5タブ廃止＝入口は図鑑・妖精界・メニューの新IA） |
| v2-8 | **妖精界の配置UX**：STEP1 は擬似ランダム自動配置＋自由ドラッグ（背景プレースホルダ）。本番は**コレットが嬉しそうに置くアニメ**・重なり回避・地面/棚などアンカー・透過アイテムが背景から浮かないか（`ART_STYLE_BLOCK` 一貫性）を実機で詰める | STEP5 | ⬜ |

- ※旧 `codex/` は `album/` に置換。妖精サイズ #1（ホーム中央で大きく）は v2 でも有効（会話・妖精界で存在感）。
- ※v2-1/v2-5/v2-6 は STEP1（1e/1f）で暫定実装済＝仕上げは STEP5／マネタイズ STEP。
