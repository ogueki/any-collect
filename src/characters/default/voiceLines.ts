/**
 * コレットの**リアクションボイス**（感情ごとの短い掛け声・相槌）の自動収録リスト。
 *
 * ホームの会話は返事を**文字で出し、声はこの掛け声を鳴らす**（spec §4.5）。
 * 返事は AI の自由文なので事前収録できないが、**声＝反応／文字＝内容**と役割を分ければ
 * 何を返しても矛盾しない。実行時ゼロ円で、生成待ちも無い。
 *
 * ## 音声の置き場と、鳴らし方
 * 音声は `src/characters/<id>/voice/<感情>/*.mp3`。**クライアントはフォルダの中身を全部拾って
 * ランダムに1本選ぶ**（直前と同じものは避ける）。**ファイル名も本数も自由**で、この一覧に
 * 載っている必要はない＝立ち絵の `sprites/<感情>/` とまったく同じ流儀。
 * 増やすほど繰り返し感が減る。収録が無い感情は黙る（動的TTSにはしない）。
 *
 * ## この一覧は何か＝「`npm run voice:record` に録らせる分」だけ
 * **いまは空＝掛け声は全部あなたが自分で作って `voice/<感情>/` に置く運用**（2026-08-14 の決定）。
 * 空なので収録スクリプトは掛け声に一切触れない＝**手で置いた音声が録り直されたり消されたりしない。**
 *
 * ここに文面を書くと、その分だけ自動収録が復活する（ファイル名は文面のハッシュ）。
 * **その場合の約束**：
 * - 文面を消す → その録音も消える（**手で置いた音声は manifest に載らないので巻き込まれない**）
 * - mp3 だけ消す → 次の実行で録り直される
 * - 直したら `npm run voice:record` を実行してから commit
 */
export const REACTION_LINES: Record<string, string[]> = {}
