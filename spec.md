# えにこれ／ 妖精コレットと過ごす、日常をちょっと冒険にする相棒アプリ ― 仕様書

**このファイルは「いま何がどうなっているか」だけを書く。**「いつ・なぜそう決めたか」「試して外した案」は [DECISIONS.md](./DECISIONS.md)、STEP の進行は [ROADMAP.md](./ROADMAP.md)、開発規約は [claude.md](./claude.md)、レイアウトの気づきは [UI-NOTES.md](./UI-NOTES.md)。

## 1. コンセプト
妖精の相棒 **コレット** と一緒に、いつもの毎日を「ちょっとした冒険」に変える寄り添いアプリ。現実の景色やモノをコレットに見せ、二人の思い出（アルバム）を貯め、図鑑のエントリを召喚魔法でアイテムにして **コレットのたからばこ**（妖精界にある宝箱の中）に持ち込む。

- **カメラモード**：外で見つけたモノ・景色を撮ってコレットに見せると、**声つきでひとこと反応**し、**写真をアルバムに保存**する。同時に**写っている主役を判定して、その部分だけ切り出し「図鑑」に集める**（Seek／iNaturalist 型・無料で取り放題）。
- **ホームモード**：中央のコレットと **会話**（記憶と好感度で少しずつ関係が育つ）が主役。入口は **図鑑・たからばこ・メニュー ＋ カメラ切替**。**図鑑**から **召喚魔法**（まほうパワー満タンで解禁）でエントリを **1日1個** 透過アイテムにして **たからばこに入れ**、**メニュー**の **妖精の窯** で2つのアイテムを合成する。アルバム／ゲームもメニューから。

**背骨（最重要の設計軸）**：**コレットの"欲・好奇心"がユーザーを現実へ連れ出す**。「コレットが行きたいって言うなら行ってみるか」。反応するだけの相棒ではなく、**世界へ連れ出す相棒**。

**二重構造**（iNaturalist 型：観察ログ × ライフリスト）：
- カメラ → 写真 → **アルバム（＝あなたの世界の観察ログ／体験の記録）**／同時に主役を判定・クロップして **図鑑（＝実物のライフリスト・種別に集約）** に収集
- **図鑑のエントリ** → 召喚魔法（まほうパワー配給）→ 透過アイテム → **たからばこ（＝コレットの宝箱／あなたが持ち込んだもの）**
- **召喚魔法＝2つの世界をつなぐ橋**。**安い収集（図鑑＝取り放題）と高い生成（アイテム化＝配給）の二層**が経済の骨格。**妖精の窯**は持ち込んだアイテム2つを混ぜる**合成**（メニュー内）。

体験の核：**「相棒コレットとの日常」×「現実世界へのリンク」**（cozy／Pikmin Bloom 的な"なんとなく今日も一緒に過ごす"路線。ソシャゲ的なゲーム性で引っ張らない）。

## 2. ターゲット / プラットフォーム
- ブラウザWEBアプリ（スマホ・PC両対応、モバイル優先）。
- 将来 **Capacitor** でネイティブ化（iOS/Android）。**主目的＝プッシュ通知＝「コレットから話しかけて毎日開く理由」**（retention の肝）。設計段階からネイティブ移行を阻害しない。

## 3. 全体構成（2モード）
| モード | 役割 | コレットの表示 |
|---|---|---|
| カメラモード | コレットに世界を見せる（撮影→声つき反応→アルバム保存＋図鑑に収集）＋コレットの"欲"の提示 | 画面右下に小さく、撮影に反応 |
| ホームモード | 会話（主役）・図鑑（召喚魔法＝アイテム化）・たからばこ・メニュー（窯＝合成／アルバム／ゲーム） | 画面中央に大きく（作業画面は右下） |

ナビ＝左上のモード切替＋入口ドック（`App.tsx`・`appStore.screen`）。作業画面の右下コレットは `WorkingScreen` 共通シェル。

## 4. 機能仕様

### 4.1 カメラモード
1. **見せる → 判定 → 図鑑に収集＋アルバムに保存**
   - ライブカメラで撮影。撮影画像を `IdentifyProvider`（`/api/identify`）経由で Gemini に渡し、コレットの **ひとことコメント＋感情** と **主役の同定情報**（名前・種キー・被写体そのものの客観的な説明・カテゴリ・bbox）を1回で生成。**声つき（動的TTS）で反応**（persona 参照）。
   - **図鑑に収集**：bbox で撮影フレームの**主役部分だけをクライアント側 canvas でクロップ**して登録。**種別デデュープ**（`speciesKey`）で同種は1マスに集約し「発見回数」を積む。**初発見は「はじめて見つけた！」演出**。**収集は無制限・無料**（配給は高価なアイテム化だけ＝§4.3）。景色だけ・不鮮明で主役が採れないときは図鑑に登録しない。
   - **写真をアルバムに保存**（既定ローカル・opt-in でクラウド＝§9）。**＝シャッター1回で「アルバム＋図鑑」の両方**に残る。**判定に失敗しても写真はアルバムに残す**。
   - 屋外・細い通信環境を想定し、**テキスト先行 → 音声後追い**・楽観的UI・待ちは in-character。画像はダウンスケールして送る。
   - **コレットは「電波」「通信」などの語を使わない**（妖精界に機械が無いので彼女は通信の概念を知らない）。「声が届かなかったみたい」のように世界の中の出来事として言う。技術的な事情は画面側の注記で出す（`CAMERA_HINT_TEXT` と同じ役割分担）。文面は `src/lib/character/failureLines.ts`。
   - 主役の説明（`caption`）は**図鑑エントリの解説だけ**に使う（アルバムには出さない＝§4.2-3）。コレットの生セリフ `comment` は**撮影時の吹き出し＋アルバム詳細**に使う。**危険対象（きのこ・野草等）の食用可否・毒性・薬効は書かせない**。
   - **コレットの反応は会話履歴にも積む**＝家に帰ったコレットが「さっき見せてもらったのは〜」と言える（カメラと家で別人にならない）。積み方と蒸し返し防止は §4.2「会話の連続性」。
   - `SceneProvider`（`/api/describe-scene`＝図鑑に残さない景色ひとこと）は**導線なしで残置**（別用途で再利用可）。
2. **コレットの"欲・好奇心"（背骨）**
   - コレットが「行きたい・見たい」を漏らしてユーザーを現実へ連れ出す。**達成リスト型のミッション/クエストUIにはしない**（cozy・streak 厳禁と正面から矛盾するため）。**日々の会話の中で自然に漏らす**＝時間/天気/場所（opt-in）＋記憶/好感度に接地して「雨だね、窓の外が見たいな」のように出す。無視してOK・罪悪感ゼロ。**＝独立機能ではなく「良い会話＋接地」の創発物**（実装場所は会話パイプライン＝§4.4）。

### 4.2 ホームモード
1. **会話（メイン）**
   - 中央に大きなコレット。テキスト入力で会話（初期 Gemini・将来 Claude に切替可）。`persona.md` の口調・性格を維持。**返事ごとに感情で表情切替**（`responseSchema` で `text`＋`emotion`＋`voiceDirection`）。感情語彙は `CharacterRenderer` の単一ソースに従う。
   - **接地（グラウンディング）**：コレットは"無から"話さず**あなたの世界の何か**から話す。燃料源＝①アルバムの写真 ②収集傾向 ③文脈（天気/時間/場所 opt-in）④共有した記憶 ⑤コレット自身の好奇心 ⑥関係の成長。`api/chat.ts` の system prompt に注入（§4.4）。
   - **会話の続け方＝一問一答にしない**：`persona.md`「会話の続け方」節（受けてから一言足す／質問は2回に1回以下・1返答に1つまで／話が一段落したらコレットから話題をふる／長さに揺らぎ／例文はそのまま使わない／一人称「わたし」はひらがな固定）＋ `buildSystemPrompt` の会話契約（「会話を前に進める」・返答は1〜3文）の二段で担保。
   - **コレットからの第一声（opening）**：**前回から間が空いたとき**にコレットの方から話しかける＝`chatStore.openConversation`（セッション1回・生成中にユーザーが先に話したら捨てる）→ `/api/chat` に `mode:'opening'`。`reunionBucket` が `first`／`back`／`days` を返し、**30分以内の再訪では出さない**（リロードは静かに続きから）。`days` は"来なかったことを咎めない"を明示。失敗時は固定挨拶にフォールバック。
   - **時間帯の接地**：クライアントが現地時刻→「朝/昼/夕方/夜/深夜」を全会話リクエストに載せ、サーバは allowlist 検証して注入（「さりげなく反映・毎回は言わない」）。
   - **「いまの話題」（`topicNote`）＝この1リクエストにだけ載る接地**。アルバム/図鑑から「これの話をしたい」と持ち出したときに、名前・どのくらい前か・そのときコレットが言ったことを短いノートで渡す（`sanitizeText` で300字まで）。**記憶ファクト・接地ノート（毎回載る）とは別枠**＝毎回載るものに入れた情報は必ず毎回言及されるため（DECISIONS 2026-08-09）。次のターン以降はコレットの返事が履歴に残ることで文脈が続く。
   - **会話の連続性**：履歴を **localStorage に永続**（`anycollect.chat.v1`・最大60件）。**モデルに送るのは直近12件だけ**（＝2×`CONSOLIDATE_EVERY`。窓から出る頃には記憶へ要約済み＝トークン頭打ち／サーバ側にも上限20件×1000字）。**切り詰めは要約済みの分だけ**（絶対上限200件のみ安全弁として強制削除）。**送信に失敗した発話は履歴に残さない**（入力欄には残るので押し直せる）。**「会話を消す」**をログのシートに常設（§9）。
   - **カメラでの発言・召喚のひとことも同じ履歴に積む**（`origin='camera'`／`'summon'`・`appendCameraLine`／`appendSummonLine`。API は叩かず、もう喋ったものを残すだけ）。ただし**記憶の要約対象からは外す**＝ファクト化すると毎回プロンプトに載り、「前に撮ったもの」を機械的に蒸し返すため（履歴として流れて消えるのが正しい）。「要約済み」のカウンタだけは前進させる（でないと切り詰めが進まない）。触れすぎ防止は `persona.md`「毎回は触れない」＋第一声プロンプトのガードと合わせて**二段**。オンボの1枚目は積まない（固定セリフは会話ではない）。
   - ※副作用＝**撮った直後にホームを開くと第一声が出ない**（カメラの発言が「最後の発話」になり 30分以内＝会話の続き扱いになる）。挨拶し直さず続きから始まる。
   - 声＝§4.5。
2. **図鑑（Seek 型・実物のコレクション）**
   - カメラで判定・クロップした**実物**を種別に集めて見返す。カード＝クロップ画像＋名前＋発見回数。詳細＝カテゴリ・**被写体そのものの客観的な解説**（常体・体言止めの事典調に固定。コレットの話し言葉/一人称は使わない）・初/最終発見日。**並び替え（カテゴリ順/新しい順）＋カテゴリ絞り込みチップ**（実際に1件以上ある category だけ表示）。**開いた直後は「新しい順」**＝さっき撮ったものがいちばん上にある（カメラ→図鑑の最頻の動機が「いま撮ったやつを見たい」なので、章立てより直近の確認を優先する）。**カメラを開く主な動機**。
   - **見た目＝標本の台紙**。アルバム（カメラロール風の生写真・詰めて並ぶ・名前なし・時系列）との差別化は4軸＝**余白**（ゆったり）／**縁**（台紙の上に載る）／**名前**（ある）／**並び**（種の順で不動）。紙の質感・書体・色は §10。
   - **1件＝台紙の上に置いた「標本カード」**（**2列**）。台紙よりわずかに明るい紙を1枚敷き、**白いマットに載せた写真を左上・右下のテープで留める**＝写真がページに直接置かれず「貼ってある」ように見える。カードごと1度未満だけ傾ける（角度は id から決定的）。テープは CSS のみ（乳白の半透明グラデ＋`clip-path` でちぎった断面）。
   - **標本番号**（`F-001` 形式＝`CATEGORY_CODE` の1文字＋連番）。**カテゴリ内の初発見順**に振るので、絞り込み・並び替えでは動かない。**「カタログの品番」ではなく「その人の図鑑の何番目か」**なので、全種リスト（分母）が未定でも矛盾しない。※削除すると以降が繰り上がる（割り切り）。
   - **カテゴリ＝章**（`第N章 ◯◯`＋右に `◯種 発見`）。縦スクロールのまま「めくっている」感じを出す（見開き/ページめくりは実装しない）。章番号は「1件以上あるカテゴリ」の順で振る＝絞り込んでも動かない。
   - **各章の末尾に空きマスを数個見せる**。**空きマスにも番号を振り「未発見」と書く**（`F-007 未発見`）＝`?` だけだと単なる余白に見えるが、番号が付くと「まだ埋まっていない席」という具体的な欠けになる。空きマスは章に属するので、章に割らない**「新しい順」では出さない**。
   - **カテゴリの判子**＝丸いインクの印（番号の頭文字）。※絵文字にグレースケール/セピアを掛ける方式は「灰色の塊」になるので使わない。
   - **「これの話をする」**＝詳細からホームへ渡して会話を始める（アルバムの「この写真の話をする」と同じ経路＝`topicNote`＋クロップ画像。手がかりは名前・**いつ初めて見つけたか・何回見つけたか**・解説）。**常に枠線＝控えめ**にする＝満タンのとき出る「召喚する」（塗りつぶし）と主役を食い合わせないため。召喚は1日1回の希少な行為なので、見た目の強さで先に立たせる。
   - ※`speciesKey` は固定タキソノミが無いため LLM 生成キーに揺らぎが残る（将来 fuzzy 照合を検討）。空きマスが後退式で「終わらない」問題と、**章の分母（「◯/36種」）を出すか**は §16。
3. **アルバム**
   - カメラで保存した写真を一覧・詳細で見返す。**時系列ソート（新しい順/古い順）**。**思い出アルバム**として機能（墓場にしない）。
   - **詳細＝被写体名＋「撮ったときコレットが言ったひとこと」（`comment`）。客観的な説明（`caption`）は出さない**＝それは図鑑の仕事で、同じ文を両方に出すと「余白・縁・名前・並び」で作った差別化が中身で崩れるため。**役割＝図鑑は標本／アルバムは思い出。**
   - **「この写真の話をする」**＝詳細から**ホームへ渡して会話を始める**。**ユーザー起点**なのが要点＝コレットが毎回言及する形にすると機械的になる（§4.4「いまの話題」）。押したら返事を待たずに遷移し、待ちはホームのタイピング表示が受け持つ（楽観的UI）。会話ログでは振った発言に**サムネイル**が付く（`ChatMessage.photoId`・写真を消していたら出さない）。
4. **召喚魔法（図鑑のエントリ → 透過アイテム化）**
   - 起点は**図鑑**。**まほうパワー満タンのときだけ**、図鑑エントリ1つを選んで透過アイテムに**召喚**する。**成功でまほうパワーをリセット**（リロールなし＝1回で採用）。**図鑑エントリは消費しない**。生成は Gemini 2.5 Flash Image（`ART_STYLE_BLOCK`）＋ Gemini（名前・説明・カテゴリ・**コレットのひとこと**）。ひとことはメタ生成と同じ1回の呼び出しで出す＝**API 呼び出しは増えない**。
   - **説明文（`description`）はコレットのセリフではない**＝「1文目でモノの正体を言い切り、2文目で魅力をテンション高めに足す」コミカル寄りの説明文（例：「じゃがいもを油で揚げた料理。ホクホク、ジューシーでたまらない！」）。図鑑の解説（事典調・常体で固定）とも、コレットの口調とも別。**召喚と合成で同じ規則を共有する**（`DESCRIPTION_RULES`）＝たからばこの詳細は両方が同じ欄に出るため。危険がありうる対象（きのこ・野草等）は**おいしそうに書かない**（食用可否・毒性・薬効を断定しない）。
   - 透過は**クロマキー方式**＝サーバは単色マゼンタ背景（`ITEM_SOLID_BG`）で描き、クライアント canvas（`src/lib/image/chromaKey.ts`）で背景色をアルファに抜く。**被写体にマゼンタ/濃ピンク/紫があると穴＝既知の割り切り**。
   - 召喚＝**あなたの世界のかけらをコレットの世界に持ち込む**。成功時は演出＋コレットが大喜び（共通シェルの右下コレットが特大リアクション＋好感度上昇）。**アイテムはたからばこに入る**。
   - **"儀式"の本体は待ち画面**（`GeneratingOverlay`）＝魔法陣（逆回しの二重円）＋外から中心へ流れ続ける光の"溜め"＋円周が満ちる進捗リング＋**魔法をかけるコレット**（`casting`＝両手を広げて掲げたポーズ。`salute`・`searching` と同じ**表示専用**で、AI が選ぶ感情には含めない）。召喚も窯（合成）も同じポーズを使う。進捗が進むほど密度・速さ・明るさが上がる。**画像アセットを使わず CSS のみ**（アニメ定義は `tailwind.config.js` の `summon-pull`／`spin-slow`。方向は `--a`、開始時刻は `animationDelay` で粒ごとに散らす＝等間隔に整列すると機械的に見えるため）。地はたからばこと同じ紫。**完成の瞬間は結果カードが直接受ける**（間に別の出現演出は挟まない＝理由は DECISIONS 2026-08-12）。
   - **結果カードのボタンは「OK」1つ**。押すと**ホームへ戻り、コレットがそのアイテムにひとこと言う**＝召喚を「在庫が1つ増えた」ではなく「きみが見つけたものをコレットに渡した」で終える。セリフは `chatStore.appendSummonLine` で会話履歴に積むので、**大セリフ・表情・立ち絵のリアクションがそのまま乗る**（API は叩かない＝生成時に一緒に返っている。取れなければ `src/lib/character/summonLines.ts` の固定セリフ）。**読み上げはしない**（ホームの大セリフの既定に合わせる）。
   - **そのセリフが大セリフである間だけ、立ち絵の横に透過アイテムが浮かぶ**（`ChatMessage.itemId`）。**次の発言に変われば自然に消える**ので、消すための状態を持たない。たからばこへはホームのドックから行く。
5. **妖精の窯（合成）**（メニュー内）
   - たからばこのアイテムを**2つ選んで混ぜ、1つの新しいアイテムに合成**する。**素材は消費しない**。結果も同じクロマキー方式で透過。系譜は `syntheses` に記録。**まほうパワーは使わない**（アイテムが素材＝別レバー）。**解禁の仕組みは無い**＝最初からメニューに出ている（§4.8 は着手しない）。
6. **たからばこ（コレットの宝箱の中）**
   - 召喚・合成した透過アイテムの置き場。**妖精界にあるコレットの宝箱**で、開けると**見た目より中が広い「4次元空間」**にアイテムが浮いている。ユーザーが召喚した **~1日1個** が少しずつ増える。
   - **背景はコード生成**（深い藍〜紫のグラデ＋星屑＋底の光）＝**アート素材に依存しない**。
   - **アイテムは漂う＋掴める**：正規化座標 `realmX/Y` をアンカーに CSS `drift` でゆっくり漂い（周期・位相・奥行きの拡縮は id から決定的に散らす）、**掴むと漂いが止まってドラッグでき、離すと新しいアンカーを永続**。未配置アイテムはマウント時に id 由来の擬似ランダム座標へ自動配置（＝コレットがしまう）。タップで詳細（削除可＝失敗品の救済）。
   - **画面いっぱい**に敷く（枠・影なし＝没入感。`WorkingScreen` の `bleed`／`tone='dark'`）。**入室演出**＝宝箱に潜り込むコレットの一枚絵を約0.9秒見せ、ズームインしながらフェード（`TreasureOpening`・タップでスキップ可・絵は `transitions/treasure-open.webp`、未配置なら演出を飛ばす）。
   - 永続フィールド名 `realmX/Y` は既存データとの互換のため据え置き。画風は `ART_STYLE_BLOCK` を共有。探索マップ化は将来の拡張（§16）。

### 4.3 まほうパワー（召喚の配給）
- **コレットに宿るまほうの力**というフレーミング。**通貨に見せない**（「パワーが足りない？ ¥120で回復！」型は禁止＝cozy を毒する）。
- `gaugeStore`（value 0..100・localStorage 永続）。**会話返信+20／撮影保存+34** で貯まり、**満タンで図鑑の召喚を解禁、成功で 0 にリセット**。ホーム上部にフィル率バー（数値でなく「まほうパワー」）。
- **カメラでは常時バーを出さない**＝撮影で貯まった瞬間に「＋まほうパワー/なつき」をポップアップ＋満タン到達時に大きめのお祝い演出（ファインダーを主役に）。
- **「1日1個」の厳密な日次クロックは未実装＝ソフトペーシング**（カメラ加算の日次上限も後日）。ペーシングは実プレイで調整＝調整ノブは `GAUGE_PER_CAPTURE`(34)／`GAUGE_PER_CHAT`(20)。

### 4.4 記憶 ＋ 好感度（関係が育つ）
- **記憶＝会話の区切りで要約し、大事な部分だけ保存**。段階式：(1) **構造化ファクト**（実装済） (2) エピソード要約（後続） (3) pgvector RAG（後続・volume と価値が見えてから）。**本格RAGは最初はやらない**。
  - **構造化ファクト**＝`memoryStore`（localStorage・最大12件 `{key,value}`）。数往復ごと（`CONSOLIDATE_EVERY=6`）に `/api/memory`（persona 非依存の中立抽出器）で「直近の会話＋現ファクト→更新後の全リスト」を安価なテキスト1回で生成し、会話のたびに `ChatContext.memoryFacts` として注入＝**名前で呼ぶ・話題を覚えてる**。**`consolidate` が成功したときだけ「どこまで要約済みか」を進める**（位置はメッセージ id で解決）。
  - **抽出器は機微/特定情報（健康・正確な位置・連絡先・認証情報）を facts に入れない。** 「忘れる」で削除可（端末ローカル）。
  - **接地ノート**＝`src/lib/grounding.ts` の `buildGroundingNotes({entries,photos})` が**クライアント側で決定的に集計**（カテゴリ傾向＋最近集めた種＝図鑑、直近に撮った被写体＝アルバム。DB/API 呼び出しゼロ）→ 最大3行を `ChatContext.groundingNotes` で注入。扱いは「話の流れに合うときだけ・並べ立てない・断定しない」。会話タブ単独起動でも接地できるよう送信時に遅延ロード。
- **安全の肝＝暫定想起**：コレットは記憶を **「たしか〜だったよね？」と暫定的に想起** し、**ユーザーが訂正できる**（persona「記憶の扱い」＋注入で二重担保）。→ 誤記憶が「間違って confident」でなく「思い出そうとしてる」に見え、**訂正が bonding の瞬間になる**。
- **好感度（なつき度）**：まず「絆」1軸（`affinityStore`・localStorage・**会話+6／撮影+4／アイテム化+12**）。**上限なし＝青天井**。レベル曲線＝**Lv2=30・Lv3=100・Lv4 以降は 150 ごと**。永続キー `anycollect.affinity.v2`。
  - **記録している素材**＝`firstAt`（始めた日）／`dayCount`（**通算**で何日来たか＝休んでも減らない）／`bySource`（会話/撮影/召喚の内訳）。**合計スコア1つに潰すと二度と逆算できない**ので、意味づけより先に採取だけ始めている。
  - **レベルで口調 tier が変わる**（`persona.md`「好感度別の口調」を会話リクエストの好感度レベルで選ばせる）。**立ち絵も level-aware**（`Sprite2DRenderer` の `level` prop）。レベルアップでコレットが大喜び＋お祝い表示。
  - ⚠️ **口調 tier と立ち絵 tier は3段のまま**なので、会話・スプライトには `toneTier()`（レベルの頭打ち版）を渡す。
  - **変化は"気づける"レベルで**＝節目で新しい話題・口調が砕ける・呼び名が変わる・新しい"欲"が解禁・たからばこの反応が増える。**効く出口は口調でなく離散的で目に見える解禁**（§16／DECISIONS 参照）。多軸化（信頼/冒険心/趣味）は将来。

### 4.5 声（TTS）
- **声はキャラの根幹。無料ユーザーが声ゼロは絶対NG＝全員に声を届ける。**
- **モードで方式を分ける**：**カメラモード（外・冒険）＝常時 動的TTS**（都度生成・低頻度＝限定コスト）／**ホームモード（家・じっくり）＝テキスト＋事前収録**（実行時ゼロ円）。ホームの事前収録は2種類＝**台本の固定セリフ**（オンボ）と**感情ごとの掛け声**（会話の返事）。

#### リアクションボイス（ホームの会話）
- **ホームの返事は本文を読み上げない。**返事に付いてくる `emotion` で `voice/<感情>/` から**短い掛け声を1本ランダムに鳴らす**（直前と同じものは避ける）。**声＝反応／文字＝内容**と役割を分けるので、AI が何を返しても矛盾しない。実行時ゼロ円・生成待ちなし。
- **音声は `voice/<感情>/*.mp3` を全部拾ってランダムに1本**。**ファイル名も本数も自由**＝立ち絵の `sprites/<感情>/` とまったく同じ流儀で、置くだけで候補が増える。**画面に文字が出ないので、台本のような一致チェックは不要**（＝違うことを喋る事故が原理的に起きない）。
- **掛け声は手で作って置く運用**（`voiceLines.ts` の `REACTION_LINES` は空＝自動収録の対象なし）。文面をそこに書くとその分だけ `npm run voice:record` が録る（ファイル名＝文面のハッシュ）。**手で置いた音声はスクリプトの管理外＝録り直されも消されもしない。**
- 収録が無い感情は**黙る**（動的TTS にはしない＝ホームは声にお金をかけない）。
- **会話ログの 🔊 タップは残す**＝押した回だけ返事の全文を動的TTS で読む。普段は無料、聞きたい人だけ。
- **経路**：`api/tts.ts`（Fish `POST /v1/tts` プロキシ・鍵はサーバのみ）／`api/_lib/voice.ts` `loadVoice`（**声の差し替えは `src/characters/<id>/voice.json` 1つ**）／`TtsProvider` 実装 `httpTtsProvider`／`src/lib/audio/useSpeak.ts`（`speak(text,{expression,direction})`・`appStore.voiceEnabled` でゲート・直前再生を停止）。

#### パートボイス（固定セリフの事前収録）
- **対象＝`script.ts` の `SPOKEN_FIXED_LINES`**（読み上げる固定セリフ7本＝イントロ3・初スキャン・図鑑リビール・召喚コーチ・たからばこ）。読み上げない画面案内（`CHAT_COACH_LINE`／`SUMMON_COACH_NOTE`／`CAMERA_HINT_TEXT`）は対象外。
- **収録＝`npm run voice:record`**（`scripts/record-voice.mjs`）。台本の TS を esbuild で評価して名簿を取り、Fish で合成して **`src/characters/<id>/voice/<lineId>.mp3` ＋ `manifest.json`** を書く（どちらもコミットする）。**キャラ差し替え単位**＝新キャラは自分の `voice/` を持てばそのまま鳴る。
- **読み方は実行時と同一ルール**（演技指示を角括弧で前置／無ければ `expressionTag`）。ただし収録は `latency:'low'` を付けない＝品質優先。
- **再生＝`speakLine(line)`**。収録があれば静的アセットを即再生、無ければ `speak()`（動的TTS）へフォールバック。
- **肝＝manifest に収録時の文面・感情・演技指示を控え、再生前に今の台本と突き合わせる。** 一致しなければ動的TTS に落ちる＝**台本を直して録り直し忘れても、画面の文字と違うことを喋る事故が起きない**（非コーダーが台本を直接編集する前提）。同じ判定を収録スクリプト側でも使うので、変更が無い回は Fish を叩かない（冪等）。
- 音声はオンボの「はい」（＝`primeAudio()` と同じ操作）で7本まとめて `fetch` して温める。
- **再生の作り**＝**使い回す単一の永続 `<audio>`** をユーザー操作で一度アンロック（`primeAudio()`）→以後 `src` 差し替えで再生（毎回 `new Audio` だと操作から時間が経った再生が iOS/Chrome にブロックされる）。**ストリーミング**＝`api/tts.ts` は chunked passthrough＋Fish body に `latency:'low'`、クライアントは MediaSource／ManagedMediaSource へ逐次流し込み**最初のチャンクで再生開始**（非対応環境は Blob 全バッファへ自動フォールバック）。
- **感情表現**：返事ごとの `emotion` を `/api/tts` へ流し、`voice.json` の `expressionTag` で **Fish の感情タグを本文の先頭に前置**（タグはトークン非計上・レイテンシ増なし）。**タグは TTS 経路でのみ付与**＝会話ログ・大セリフの表示には混ぜない。感情名は小文字英字のみの allowlist で検証し、未知・`neutral` は**タグなし＝素の声**。
  - **返事ごとの演技指示**＝`api/chat.ts` の `responseSchema` の **`voiceDirection`**（日本語の自由文・任意）を `ChatMessage` 経由で `/api/tts` に渡す（**API呼び出しは増えない**）。書き方の指針は `persona.md`「声の演技指示」節＝**キャラごとに演技も差し替わる**。
  - **優先順位**＝`voiceDirection` → `voice.json` の感情別固定タグ → タグなし。**下限は常に担保**。
  - **安全**＝LLM 由来の自由文が TTS に入るため、サーバ（`sanitizeDirection`）で**角括弧の除去・改行の圧縮・60字上限**を必ず通す（タグの入れ子/脱出を防ぐ）。本文側も角括弧を除去する。
  - **タグの書き方は DECISIONS「Fish TTS の感情タグは日本語の自由文で書く」を必ず読む**（英語タグは効かない）。
- **声そのものの切替も同じ経路**：`voice.json` に `variants`（名前つき別音声）＋`expressionVariant`（感情→variant）を書くと、その感情だけ別の声で喋る。**タグ主体・声切替主体・併用のいずれもコード無改修で JSON だけ**で切り替わる。
- **正典ボイス＝Fish Voice Design で生成したオリジナル合成音声**（実在人物に基づかない＝**権利クリーン・クレジット表記も不要**）。検証中のモデルは無料の `s2.1-pro-free`（free/pro は同一モデル＝音質・単発速度同じ、差は SLA/非学習/**無料提供は 2026-08-31 まで**）。

### 4.6 妖精キャラクター
- **コレット**＝デフォルト妖精。**好奇心旺盛・冒険好き・アクティブ**（主導的に話題・欲・意見を出す。ただし cozy であって needy でない）。3頭身ちびの2Dイラスト。
- 表示は抽象化レイヤー越し（`CharacterRenderer`／将来 Live2D/3D 差し替え可）。**キャラは差し替え単位**：`src/characters/<id>/`（`persona.md` ＋ スプライト ＋ `voice.json`＝声の設定 ＋ `voiceLines.ts`＝掛け声の文面 ＋ `voice/`＝収録音声 ＋ 背景 ＋ transitions）。
- **キャラ統一の原則**：アイテム名/説明/反応/会話の全AI呼び出しが `persona.md` を参照。

### 4.7 オマケ：アイテムで遊ぶミニゲーム
集めた**透過アイテム**を使ったミニゲーム群。**メニュー**（`MenuSheet`）から**全画面 overlay**で起動＝ホームのタブは増やさない。中核（コレット＝相棒）を邪魔しない**オマケ**。ベストは localStorage。**物理は固定タイムステップ**（1000/60ms）。**解禁の仕組みは無い**＝最初からメニューに出ている（§4.8 は着手しない）。

- **タワーバトル**（「積んで遊ぶ」）：透過アイテムを物理で積む（動物タワーバトル型）。**ソロ・エンドレス**＝ドラッグで狙って落とす→場外落下で終了＝積めた数がスコア。**VSコレット**＝交互ターンで、**落とした一手で場外落下が起きたらその手番の側が負け**（コレットは山の重心±ノイズを狙う簡単AI）。物理＝`matter.js`（**遅延ロード**）、当たり＝透過PNGのアルファから**凸包**（`alphaShape.ts`・縮退時は矩形）、描画＝自前 canvas（重心オフセット補正）。
- **フラッピー**（「とんで遊ぶ」）：主役アイテムを1つ選び、タップではばたいてパステルな柱の隙間をくぐる。matter.js 不要の自前ループ。**難易度は一定**（進行によるランプを持たない）＝本家の定数を**縦は空の高さ・横は画面幅で相似スケール**し、**柱の周期は px でなくステップ数で固定**（84 step＝1.4秒）。当たりは**アルファ凸包の外接矩形 × 0.92**。
  - **難易度の調整ノブ＝`REF_GAP`（隙間）と `birdSizeFor` の係数（主役＝当たりの大きさ）**。`GAP_PER_BIRD = 2.5` は横長画面用の安全下限で通常は効かない。実測値と根拠は DECISIONS。
- 将来＝凹み当たり判定/回転操作/背景アート/AI強化／障害物アート/主役ごとの当たり形状。

### 4.8 ショップ（機能アイテムの解禁）※構想・**着手しない**
> **社内公開の時点では過剰**として段から外してある（判断＝DECISIONS 2026-08-15）。**解禁対象は3つ**（窯・タワー・フラッピー）で、メニューは4行しかない。**機能が増えたときか、マネタイズ（ROADMAP 8）に着手するときに戻す。** 以下は戻すときのための構想。
> 段階的開放だけが欲しいなら、**コレットからの贈り物**（下記のアルバムと同じ方式）のほうが安く、関係性の製品に合う。
- **機能を「アイテム」としてショップ購入で解禁する**。入口はメニュー内を想定。
- **対象＝付加機能のみ**：妖精の窯（§4.2の5）／妖精の切り株（タワーバトル）／フラッピー用の別アイテム（名称TBD）／将来の大型機能（マップ→「妖精の地図」等）。**解禁単位はゲームごとに別アイテム**。
- **対象外＝コアループ（カメラ・会話・図鑑・召喚魔法）＋アルバム**。アルバムは購入でなく**コレットからの贈り物**（序盤の無償イベント）＝「記憶を人質にしない」原則（§9）の遵守。
- **通貨＝新ゲーム内通貨**（名称TBD）。**まほうパワー（§4.3）とは別財布**。コアループの行動で獲得。**実課金販売はしない**。**常設カウンターは置かない**＝ショップ内でのみ表示。
- **おねだり規律**：コレットがおねだりしてよいのは**ゲーム内通貨で買える品のみ**。実課金品・サブスクは絶対に催促しない。発生も抑制的に＝「あと少しで買える品があるとき、opening で時々触れる」程度・会話中の売り込みはしない。
- **解禁の反映＝当面メニューのみ**。ただし**購入演出でモノ感を担保**（コレット大喜び＋「運んできた」→メニューに新しい行＝追加アート不要）。
- **「設備」と「どうぐ」の概念区別**：設備＝いつかホームに置かれる物理的なモノ（窯・切り株）／どうぐ＝メニューやアイコンから使う能力（地図等）。
- **placement 布石**：所有データは `placement`（任意項目）を持てる形にし、当面は無視。将来「コレットの部屋・庭」アート（§16）導入時に**表示層の変更だけ**でホーム設置へ移行できるように。
- 経済数値・カタログ演出・登場タイミングは §16。

### 4.9 オンボーディング（初回体験）
- **コレット主導の初回オーバーレイ**＝`features/onboarding/`（`OnboardingOverlay`＋台本 `script.ts`＋`onboardingStore`）。声は事前収録のパートボイス（§4.5）、立ち絵/背景は既存流用＝**新規アートなし**。第一声（§4.2）は完了までゲート。`?debug=1` のメニューに「オンボもう一度」。
- **台本は全て `features/onboarding/script.ts`**（`ONBOARDING_STEPS`／`CAMERA_HINT_TEXT`／`FIRST_SCAN_LINE`／`COLLECTION_REVEAL_LINE`／`SUMMON_COACH_LINE`＋`SUMMON_COACH_NOTE`／`TREASURE_REVEAL_LINE`／`CHAT_COACH_LINE`。`OnboardingStep` 型で声・表情が自動。読み上げる分の名簿＝`SPOKEN_FIXED_LINES`）。音声選択画面のあいさつのみ `OnboardingOverlay.tsx` 直書き。
- **ルールの説明はコレットにさせない**＝仕組みの説明は `SUMMON_COACH_NOTE` のような画面側の注記に分離する（`CAMERA_HINT_TEXT` と同じ役割分担）。"召喚"のような世界内の概念は彼女が言う。
- **流れ**：
  1. **音声の選択**＝「音声を再生しますか？（あとで変更できます）」→はい/いいえ。**この画面には世界観のナレーションを置かず、立ち絵も出さない**（時間帯背景＋質問だけ）。**「はい」のタップの中で `primeAudio()`** ＝選択がそのままアンロックの user gesture を兼ねる。選択は `appStore.setVoice`→localStorage `anycollect.voice.v1` に永続（`toggleVoice` も永続＝各画面の 🔊 が「あとで変更」を担う）。
  2. **コレット登場＝イントロ3枚**（はじめまして → きみの世界に興味がある → 見せてほしい →「カメラをひらく」）。前置きは挟まない。
  3. **撮影ガイド**＝`phase='shoot'` の間だけ、カメラに吹き出し＋シャッターのピング＋声かけ（`CAMERA_HINT`）。
  4. **図鑑リビール**＝初スキャン後にコレットが `FIRST_SCAN_LINE`「図鑑に書いておくね」→カメラに**手渡しカード**（スキャンしたクロップ＋被写体名＋「図鑑をひらく」）→図鑑を初めて開くと `COLLECTION_REVEAL_LINE`「ふたりで図鑑をいっぱいにしよう」のヒーローリビール。**メインコンテンツ＝「きみとコレットの図鑑をつくる」**を伝える山場。
  5. **召喚コーチ**（まほうが初めて満タンになった図鑑）→ **たからばこ**（初召喚の宝物が背後に漂う初訪問）→ **会話コーチ**（一巡後のホーム初回・第一声とぶつからないよう**読み上げない画面案内**）。
- **実装の肝**：
  - **収集に成功した回だけ reveal へ進む**（判定/収集失敗は `shoot` のまま＝撮り直せる・空の図鑑を指す行き止まりを防ぐ）。
  - **コーチは phase の一本道でなく永続の seen フラグ3種**（`anycollect.onboarding.{summon,treasure,chat}.v1`・`resetOnboarding` で再武装）＝リロード/翌日でも各画面の初回で必ず一度出る。
  - **表示は store の seen からレンダー時に導出する**（effect 内で local setState しない＝`react-hooks/set-state-in-effect` を踏まない）。閉じるときに `mark…Seen()` で永続。
  - **まほうパワーはシードで配らない**＝撮影+34/会話+20 で自然に貯める。**"空で放り出さない"は図鑑に既に1件あることで達成**。
  - アイテムを指す**「子」呼びは使わない**（非人称化）。
- 二次機能（アルバム/窯/ゲーム）はオンボに載せない。**当面はメニューに出したまま案内もしない**（§4.8 を着手しないため、文脈導入の受け皿が無い）。入れるなら**コレットからの贈り物**が第一候補。
- **台本は `features/onboarding/script.ts` に集約**（非コーダーが直接編集できる素のテキスト）。**文面・演技指示を直したら `npm run voice:record`**（＝パートボイス／§4.5。回し忘れても動的TTSに落ちるだけで壊れない）。

## 5. AI構成
| 用途 | サービス | 備考 |
|---|---|---|
| 召喚・合成（図鑑エントリのクロップ／アイテム2つ→透過アイコン） | Gemini 2.5 Flash Image | 約 $0.04/枚＝¥7/回。**まほうパワー配給（実質1日1個）**で制御。`ART_STYLE_BLOCK` 共有 |
| 反応・会話・名前/説明・記憶要約 | **Gemini（初期）→ Claude（将来切替可）** | `ChatProvider`/`SceneProvider`/`IdentifyProvider`/`MemoryProvider` 抽象。口調は persona.md で統一 |
| 音声合成 | **Fish Audio** | $15/100万UTF-8バイト＝日本語 ¥0.007/字。カメラ＝動的／ホーム＝事前収録（§4.5）＝実行時ゼロ円。🔊 タップ時のみ動的 |

**コスト最適化（必須）**：①プロンプトキャッシュ ②履歴の要約＋窓（**送信窓＝直近12件は実装済**）③接地は top-k のみ ④画像ダウンスケール ⑤TTS 規律 ⑥レート制限/日次上限（**現状は未実装＝§12**）⑦プロバイダ抽象で安いモデルに差替可。
- 実額（$1≈¥150）：会話1ターン≈¥0.15〜0.25／カメラ反応≈¥0.4〜0.5／アイテム化¥7。最適化後の月コスト＝ヘビー≈¥210・ライト¥30〜60 → サブスクで黒字（§15）。※価格は変動、実装時に最新確認。

## 6. データモデル（Supabase / Postgres）
> **現状：永続化は IndexedDB / localStorage 先行。** 下記 Supabase スキーマは最終形で、同じ抽象の裏に STEP6 で追加する（抽象は不変）。

- 認証：**匿名認証**。引き継ぎは **opt-in メール/パスキー**。全テーブルに **Row Level Security**。
- テーブル（案）
  - `profiles`：匿名ユーザー設定（選択中キャラ、音声ON/OFF、好感度、opt-in 状態 等）
  - `items`：`id, user_id, name, description, category, icon_url(透過), source_collection_id, created_at`（`category` は固定キー `food/creature/nature/gear/toy/wear/other`。**召喚/合成で生成した透過アイテム**＝たからばこに入る。※現行 `Item` 型の `sourcePhotoId` は旧「写真からアイテム化」の名残＝未使用）
  - `collection`：`id, user_id, species_key, name, description, category, image_ref(クロップ), count, first_seen_at, last_seen_at`（**図鑑**・種別デデュープ）
  - `photos`：`id, user_id, storage(local/cloud), url_or_ref, created_at, meta`（**アルバムの写真**）
  - `memories`：`id, user_id, kind(fact/episode), content, weight, created_at`（pgvector 列は後続）
  - `affinity`：`user_id, axis, value, updated_at`
  - `syntheses`：`id, user_id, result_item_id, parent_a_id, parent_b_id, created_at`
  - `conversations`：`id, user_id, role, content, created_at`（任意）
- Storage：生成アイコン用 `item-icons` ／ **写真用 `photos`（opt-in クラウド時のみ）**。
- **データ2クラス**：関係データ（好感度・記憶要約・アイテムメタ＝小・低リスク）は既定クラウド／**生写真（大・高リスク）は opt-in クラウド（既定ローカル）**。

## 7. 技術スタック
- フロント：React 19 + TypeScript + Vite + Tailwind CSS + Zustand
- API層：Vercel Serverless Functions（**APIキーはここに集約**）
- バックエンド：Supabase（Postgres / Storage / Anonymous Auth）※STEP6
- AI：Gemini（画像・定型テキスト・会話）／ Fish Audio（音声）。会話は将来 Claude に切替可。

## 8. アーキテクチャ / 拡張性
拡張・ネイティブ化を見据え、以下を**インターフェースで抽象化**する：
- **AIプロバイダ**：`ImageGenProvider` / `ChatProvider` / `SceneProvider` / `IdentifyProvider` / `TtsProvider` / `MemoryProvider`
- **キャラレンダラ**：`CharacterRenderer`（2Dスプライト、将来 Live2D/3D）
- **ストレージ**：Repository パターン（`ItemRepository`/`PhotoRepository`/`CollectionRepository`。**インターフェースを先に切って** IndexedDB↔Supabase を吸収）
- **キャラクター定義**：`characters/<id>/`（persona・スプライト・voice・背景）

### ディレクトリ構成
```
any-collect/
  api/
    identify.ts         # Gemini: 図鑑判定（主役同定＋bbox・Seek型）
    chat.ts             # Gemini: 会話（接地注入）／将来 Claude
    generate-item.ts    # Gemini: 召喚＝図鑑エントリ→透過アイテム
    synthesize.ts       # Gemini: 窯＝2アイテム合成
    tts.ts              # Fish Audio: 音声
    memory.ts           # 会話→記憶ファクト抽出（保存はクライアント側）
    describe-scene.ts   # Gemini: 景色ひとこと（導線なしの残置）
    _lib/               # http（共通ガード＝入力検証/サイズ上限/エラー隠蔽）
                        # persona/gemini/gemini-image/fal-image/item-prompt/voice
  src/
    components/         # モード横断の共有UI（WorkingScreen/MenuSheet/GeneratingOverlay/icons）
    features/{camera,home,collection,album,kiln,treasure,game,onboarding}/
    lib/
      ai/{imageProvider,chatProvider,sceneProvider,identifyProvider,ttsProvider,memoryProvider}.ts
      audio/useSpeak.ts # TTS 再生（永続 audio・MediaSource ストリーミング）＋`speakLine`
      audio/partVoice.ts # 事前収録の索引＋台本との突き合わせ（§4.5 パートボイス）
      character/{CharacterRenderer.ts,Sprite2DRenderer.tsx,waitLines.ts,failureLines.ts,summonLines.ts}
      image/{crop,chromaKey,alphaShape}.ts  # クロップ（図鑑）／透過化（召喚・合成）／凸包（ゲーム）
      grounding.ts      # 図鑑・アルバム傾向→会話の接地ノート
      storage/{itemRepository,photoRepository,collectionRepository}.ts  # memory/affinity は STEP6
      supabase/client.ts  # STEP6 で追加予定
    characters/default/{persona.md,sprites/,backgrounds/,transitions/,voice.json,voiceLines.ts,voice/}
    store/    # app / chat / album / collection / codex / gauge / affinity / memory / game / onboarding
    styles/  types/
  public/
  .env.example
  spec.md  DECISIONS.md  ROADMAP.md  UI-NOTES.md  claude.md
```

## 9. プライバシー方針
- **写真は保存する（アルバム）**。ただし **既定はローカル端末のみ**、**クラウド保存は opt-in**。
- **引き継ぎ（記憶・写真の永続）＝opt-in メール/パスキー**。匿名のまま使う人は登録不要＝「全員から強制的に PII を集めない」。
- **エクスポート・削除を一級機能**にする（"記憶を人質"にしない自制を設計で明示）。
  - **実装済（端末ローカル分）**＝**記憶の「忘れる」**（`memoryStore.forget`）と**会話ログの「会話を消す」**（`chatStore.reset`）。**対象が違うので導線を分ける**（記憶パネルの見出し行／会話ログの見出し行）。エクスポートとクラウド分の削除は STEP6。
- **モデレーション／安全**（§12）：写真の安全チェック（NSFW/違法）、**顔の扱い**（人物同定・不気味コメント禁・ぼかしオプション）、**入力の許容範囲**（他人の顔/ブランド/版権＝肖像/IP／各社 ToS 確認）、**クライシス層**（自傷・危機→ケア＋相談先・RPで流さない・年齢配慮）、**キャラ崩れフォールバック**。
  - **キャラ崩れフォールバックは実装済み**＝会話・召喚・窯・保存の失敗を `src/lib/character/failureLines.ts` の**コレットのセリフで受ける**（`waitLines.ts` と同じ流儀＝非コーダーが直接編集できる素のテキスト）。**ルール＝失敗の原因を説明させない／UI 操作を指示させない／読み上げない**（通信断で TTS を取りに行くと二重に失敗する）。図鑑・窯は立ち絵の表情も合わせる。**カメラの判定失敗にはセリフを置かない**。他の4項目（NSFW/顔/許容範囲/クライシス層）は未着手＝STEP7。
- APIキーはサーバ側（Vercel Functions）のみ。全テーブル RLS（STEP6）。
- **API の入力検証**＝`api/_lib/http.ts` に全ハンドラ共通のガードを集約。**新しいエンドポイントは必ずここを通す**。
  - `personaId` は allowlist で検証（`resolve()` に渡す前に弾く＝パストラバーサル対策・loader 側でも二重防御）
  - リクエストボディと画像 data URL にサイズ上限（超過は 413）
  - プロンプトに載るクライアント由来の自由文字列（記憶ファクト・接地ノート・合成素材の名前と説明）は制御文字を潰して長さで切る
  - **エラー詳細をクライアントに返さない**（Gemini・Fish の生エラーはアカウント情報を含みうるのでサーバログのみ）
  - TTS は本文からも角括弧を除去（Fish の感情タグ注入を防ぐ）

## 10. デザイン
- トーン：ポップ＆カラフル、丸み・余白・やわらかい影。cozy。
- フォント：日本語＝M PLUS Rounded 1c / Zen Maru Gothic、英数＝Fredoka / Baloo 2。
- パレット：ミント `#6EE7B7` / ラベンダー `#C4B5FD` / ピーチ `#FDA4AF` / レモン `#FDE68A` ＋ 白／やわらかグレー（`tailwind.config.js` にトークン）。
- **図鑑だけの「紙」**：`paper #F3E9D2` / `paperEdge #D9C9A8` / `ink #4A3F32` ＋影 `shadow-paper`（標本の厚み）・`shadow-sheet`（台紙の面）・`shadow-specimen`（貼った標本）。**この3色と影は図鑑でしか使わない**＝ほかの画面は白＋パステルのままにして「図鑑は別の場所」を色で伝える。
  - **紙は周囲より濃くする**。body の背景グラデはほぼ白なので、白っぽい紙では「紙が置かれている」ことすら見えない。
  - **書体は明朝**（`font-zukan`＝Shippori Mincho）。既定の sans が丸ゴシックなので、同系の丸ゴシックでは差が出ない。
  - 質感は**画像アセットを使わず CSS だけ**（`.zukan-paper`＝地の色＋`feTurbulence` のノイズ）。**ノイズの SVG には `width`/`height` を持たせて `background-size` でタイルする**（無いと器の全面に引き伸ばされて粒が潰れる）。**opacity は 0.15 前後**。
  - **台紙は角丸を落とし、影は内側へ**（外に広げると「浮いたカード」になって紙に見えない）。**標本は白フチ（マット）ごと 1 度未満だけ傾けて貼る**（傾きは id から決定的に出す）。**紙の上にアプリのピル（丸い紫＋光る影）を置かない**＝チップはインクの矩形にする。
  - **罫線を px 固定にしない**（`html { font-size: clamp(...) }` で全体がズームするため）。
- **たからばこの中**：4次元空間にアイテムが漂う収納ビュー（背景はコード生成）。アイテム化・好感度上昇は色とアニメで気持ちよく。
- レイアウトの決着事項（画面高の吸収・背景の上の文字・光の付け方）は [UI-NOTES.md](./UI-NOTES.md)。

## 11. ネイティブアプリ化の方針
- **Capacitor** 採用。**提出（社内Web）後すぐネイティブ化**し、**プッシュ通知＝毎日フックの配信（retention 本番）**／ネイティブカメラ・OS／梱包を入れる。
- まず PWA を維持。ネイティブ機能はアダプタ層越し。

## 12. 非機能要件
- 生成中/反応中のUX：コレットの演出でローディング（悪通信フォールバック含む）。
- エラー時：AI 失敗・API不通でもコレットのセリフで自然にフォロー（§9）。
- コスト管理：§5 の最適化＋まほうパワー（実質の栓）。
- **⚠️ 呼び出し回数の制限（レート制限・日次上限）は未実装**。`api/` は**無認証で公開されており、URL を知れば誰でも到達できる**（アクセス制御は無い）。判断の理由と**再検討のトリガー**は [DECISIONS.md](./DECISIONS.md#api-の回数制限レート制限日次上限は入れない)。

## 13. ロードマップ
> STEP 単位の進捗は [ROADMAP.md](./ROADMAP.md)（そちらが正）。v2 は **コアループ先行（local）→ 土台/記憶/声/安全/課金** の順で積む。

## 14. 製品の方向性・リテンション設計
- **北極星**：「いつもの毎日を、ちょっとだけ冒険にしてくれる相棒」。2軸＝①相棒コレットとの日常（一緒の時間が関係に蓄積）②現実世界へのリンク（日常が違って見える）。
- **背骨＝コレットの"欲"が現実へ連れ出す**（§1）。#1毎日フック（今日コレットが行きたそう）／#2接地（欲は共有世界・記憶から）／報酬（持ち帰る→たからばこに出現→大喜び・記憶・好感度）／リテンション（ポイントでなく"関係"で駆動＝Pikmin Bloom 型を感情で）を一本に束ねる。
- **毎日開く理由**：主フック＝**記憶ベースの挨拶**（"昨日の夕日まだ覚えてるよ"）／**会話でコレットが漏らす"欲"**（達成リストにはしない）／アルバム記念日／**今日の1個（召喚）**。Web 期間はアプリ内表示＋可能なら PWA 通知、本番はネイティブ push。
- **勝ち筋**：会話型は"燃料切れ"しやすいが、本作は **①LLMの開放的生成 ＋ ②入力源＝現実（写真・世界）＝外部由来で枯れにくい ＋ ③記憶＋好感度で"あなたへの理解"が育つ**。
- **図鑑・アルバム・たからばこを墓場にしない**：蓄積が目に見える（たからばこがあなたの持ち込みで賑わう）。

## 15. マネタイズ
- **核心的緊張**：cozy な関係 ⟂ 取引的な課金。原則＝**「二人の世界を豊かにする/守る」支出に見せる。「コレットの愛を金で買う」に絶対に見せない。**
- **モデル＝ハイブリッド**：
  - **主収入＝コスメ（世界装飾）**：ホーム背景・家具・テーマ・季節装飾＋**たからばこの空間テーマ**。現行2Dで作れる Animal Crossing 型・非搾取。**コレット本体の着せ替えは Live2D/3D 化の時代まで温める**（2D描き置きでは衣装×表情が組合せ爆発。簡易アクセサリの透過 overlay のみ可）。
  - **土台＝サブスク**：クラウド永続・端末跨ぎ／**声の余剰**（動的本文の音声化・高音質・量）／写真のクラウド容量／加入者特典コスメ。**無料でも1台で完全な関係が築ける**＝有料は"上乗せ"。
  - **機能アイテムショップ（§4.8）**：**APIコストのある機能（窯＝合成 等）→サブスク棚**／**ランニングコストゼロの機能（ゲーム等）→買い切り直販**。**通貨チャージ型は不採用**。**おねだり規律**＝実課金品・サブスクは絶対に催促しない。
  - ホーム背景を**背景IDで差し替え可能なアセット**として実装済み＝背景・テーマ販売の土台は既にある。既存所有→サブスク移行の整合は STEP8。
- **やらない**：**広告**（cozy を毒す＋広告収益/セッション ≲ APIコスト/セッション）／**ゲージ上昇率・回復アイテムの販売**（COGS逆ざや＋関係ゲージの通貨化）。
- **声は全員（§4.5）**＝有料の壁の奥に隠さない。有料は"声そのもの"でなく"声の余剰"。
- 提出（ポートフォリオ）は決済テストモードで可。

## 16. 未確定 / 将来検討
- **図鑑の空きマスが後退式**（埋まっている数＋3）で**実際には終わらない**。「終わる器」にしたければカテゴリごとの固定目標＋達成表示に切り替える。
- **図鑑に切り抜き（背景除去）を入れるか**。入れたら台紙の見立てを「薄い楕円の座布団」に差し替える。
- **なつき度レベルの"意味"**＝何が解禁されるか。効く出口は口調でなく**離散的で目に見える解禁**（呼び名／感情／たからばこの反応／声の段階解禁）。好感度の多軸化（信頼/冒険心/趣味）も。
- **ショップ（§4.8）の未決**：通貨名称・獲得レート・価格帯・解禁順序・フラッピー用アイテム名・カタログ演出の具体（案＝妖精の国の通販カタログをコレットとめくる・新キャラ＝行商人は足さない）・ショップ登場タイミング・アルバム贈り物イベントの流れ・実課金導入時の既存所有との整合（STEP8）。
- **ホーム＝「コレットの部屋・庭」アートの将来ビジョン**（デザイナー案件・時期未定）。発注/自作時の要件＝**設備（§4.8）の空き地スロット・レイヤー順（コレットが手前）・状態差分（窯に火が灯る等）を最初から含める**こと。空き地がうっすら見える＝「この世界はまだ育つ」ティーザーを兼ねる。当面版＝木のうろの部屋×時間帯4枚（背景ID `tree-hollow`）。
- **妖精界の探索マップ化**（たからばこの外＝歩ける世界を作るなら）。
- **イベント/トリガー**：状況起点でコレットが固有アイテムを贈る（迷子→コンパス 等）。位置/天気/時間はクライアント側で処理。
- **レア度は廃止済み**（LLM 主観で真値が無く cozy と噛み合わない）。必要になったら仕様を練り直して付け直す＝復元手順は DECISIONS。
- **記憶の (2) エピソード要約 ／ (3) pgvector RAG ／ 接地の top-k 検索**（§4.4）。
- **漫画演出（漫符）**：プロトタイプは破棄済み・git タグ `shelved/manpu-step11a` に保存。再開はレイアウト確定後。
- **テストが1本もない**（vitest 未導入）＝導入は保留中の判断。
