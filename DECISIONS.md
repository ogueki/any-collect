# DECISIONS ― 決定と経緯のログ

**「いま何がどうなっているか」は [spec.md](./spec.md) が正。このファイルは「いつ・何を・なぜ決めたか」と「試して外した案」だけを残す。**

- **追記専用**（新しいものが上）。過去のエントリは書き換えず、覆ったら新しいエントリで上書きする。
- 仕様書に書くと現在形が読めなくなるが、失うと同じ失敗を繰り返す——という情報の置き場。
- 迷ったときの分け方＝**値・ルール・ファイルの場所は spec.md ／ 日付・旧仕様・理由・却下案は DECISIONS.md**。

---

## 2026-08-18

### `api/` の守りは3層に分ける（層0＝予算上限／層1＝オリジン検査／層2＝日次総量）
Ⅰ.5-2「無認証を塞ぐ」の設計。**認証（STEP6）を前倒しせずに、提出までにできることだけを積む**。
- **層0＝Vercel / Google 側の予算上限**（コンソール作業・コード0）。**コード側の上限には必ず穴があるので、最終的な蓋はここに置く。**
- **層1＝発信元オリジンの検査**（実装した）＝`rejectForeignOrigin` を全7エンドポイントの method チェック直後に1行。`Origin`（無ければ `Referer`）を `ALLOWED_ORIGINS` と突き合わせ、外れたら 403。
  - **`ALLOWED_ORIGINS` 未設定なら検査そのものを行わない**（opt-in）。**設定を忘れただけで本番が全滅するほうが、素通しより害が大きい**という判断。Vercel のプレビューはホスト名が毎回変わるので `VERCEL_URL` を自動で足す。
  - ⚠️ **これは鍵ではない。** Origin は偽装できる。効くのは「URL を知った人が curl で叩く」までで、目的の半分は **「無防備で置いていたわけではない」と外から見て分かること**（08-08 に書いた「意図した／気づいていない の区別が付かない」への回答）。
  - 検証＝`esbuild` で `http.ts` を bundle して **13ケースを node で実行**（未設定は通す／本番ドメイン・末尾スラッシュ・大文字・Referer 経由・プレビュー・localhost は通す／curl・他所・**似ているが別ドメイン**・http・壊れた Referer は 403）。**本番を締め出しうるコードなので、目視でなく実行で確かめる。**
- **層2＝日次総量の上限は入れていない**。入れるとしても per-IP でなく**全ユーザー合計**（＝「1日いくらまで払うか」）で、置き場は Upstash/Vercel KV。**位置づけは「ユーザーを取り締まる装置」ではなく「クライアントのリトライ暴走に対するバグ保険」**＝人が回すかどうかとは無関係に価値がある。

### ひとりあたりの回数制限は入れない（08-04 の判断を維持）＋絞るなら日次クロック
ユーザー「社内公開でもひとりごとの回数を絞るべきだろうか。そんなに回しまくる人はいないと思ってる」。**結論＝入れない。**
- **規模**＝社内20人が1人10回召喚しても 200×¥7＝**¥1,400**。会話・図鑑判定・TTS は桁が2つ下。この額のために体験を絞るのは割に合わない。
- **08-04 の再検討トリガー（一般公開／社外配布／請求超過／STEP6）にどれも当たっていない。**
- **効く相手が違う**＝怖いのは「たくさん使う人」ではなく **①外部からの直叩き（層1で落ちる） ②クライアントのリトライ暴走（同じ人が正当に叩く形なので per-user 制限は無力＝総量の蓋だけが効く）**。
- **将来ひとりを絞るときも汎用のレート制限は使わない**＝spec §4.3 は召喚を「**1日1個**」と定めているのに**日次クロックが未実装**で、いまはまほうパワーが貯まるだけ召喚できる（撮影+34／会話+20 で満タン100）。**エラーで断るより「今日はもう魔法が満タンにならない」＝世界の中の理由で止まる**ほうがこのアプリに合う。ただし体験が変わるので、長期の目標（未決の本題①）を決めるときにセットで。

### 好感度（なつき度）で口調を変えるのをやめる＝会話から切り離した
ユーザー判断「**好感度システムは現状持て余してるから一旦消そう**」。きっかけ＝persona.md の「好感度別の口調（3 tier）」節を削ったのに、`buildSystemPrompt` は毎リクエスト**「ペルソナ定義の『好感度別の口調』のうち…」と名指しで参照し続けていた**＝存在しない節を指す指示が宙に浮いていた。
- **消したもの**＝`ChatContext.affinityLevel`／`buildSystemPrompt` の好感度の行／`api/chat.ts` の受け口／`ChatOpts.affinityLevel`／`httpChatProvider` の送信／`chatStore.gatherChatContext` の収集／`affinityStore` の `toneTier()` メソッド。**会話リクエストに好感度は一切載らない。**
- **残したもの**＝なつき度そのもの（加算・レベル曲線・永続）／ホームの Lv.N チップとバー／レベルアップのお祝い演出／**立ち絵の level-aware**（`sprites/<感情>/lv1,lv2…`＝`toneTierForLevel()` は絵の選択専用として存続）。
- **理由**＝レベルが上がっても口調が変わったと**気づけない**（3段しかなく、変化は文体の微差）。spec §16 に前から書いてあるとおり **効く出口は口調でなく「離散的で目に見える解禁」**＝そちらを作るまでは、会話プロンプトに毎回載せるだけの死んだ指示になる。
- ⚠️ **これは「なつき度をやめた」ではない**。素材（`dayCount`／`bySource`）は採り続けているので、意味づけが決まったときに出口だけ作ればよい。戻すのは `ChatContext` に1フィールド足すだけ。

### 返答の長さ・絵文字・記法は persona.md でなくシステム側が唯一の持ち主
ユーザー判断「**ここはペルソナmdじゃなくシステム側で全部制御したほうがいい**」。
- persona.md に「1返答は2〜3文」、`buildSystemPrompt` に「1回の返答は1〜3文」と**値まで食い違ったまま両方が生きていた**（同じ事実を2か所に書けば必ず片方が腐る、の実例）。
- **持ち主＝`buildSystemPrompt`**。persona.md からは長さ・絵文字・Markdown の記述を全部落とし、代わりに「ここでは指定しない」と明記した（非コーダーが編集するファイルなので、**黙って消すと"抜け落ちた"と読まれて書き戻される**）。
- **境界の決め方**＝**キャラを差し替えても崩れてはいけない規格は契約側／その子らしさは persona 側。** 文の長さ・絵文字の有無は前者（UIが壊れる）、語尾やツッコミの有無は後者。
- 「長さに揺らぎを」も persona から契約側へ寄せた（文数の話なので）。

### コレットはスマートフォンを「魔法のアイテム」だと思っている
世界観の正典を更新（`persona.md`「世界観」）。妖精界と人間界は**スマートフォンを通してのみ交信できる**設定にしたので、彼女がそれを何だと思っているかを決めないと会話で必ず突かれる。
- **結論＝機械だとは知らない。人間界のふしぎな魔法のアイテムだと思っている。** 仕組みの言葉（電波・通信・アプリ・画面・レンズ）を持たず、「この板ごしに見える」「魔法の窓」のように見たままで話す。
- ⚠️ **2026-08-04 の「電波・通信を言わせない」は結論そのまま・理由だけ差し替わった**（旧＝妖精界に機械が無いから／新＝魔法のアイテムだと思っているから）。オンボ intro-3 が既に「その『スマートフォン』で」と言わせているので、**語そのものは彼女の語彙にある**（知らないのは仕組みの方）。

### 「口癖・決め台詞」の定型4行は意図的に削除（様子見）
persona.md から 発見時／撮影を促す／アイテム化の喜び／冒険への誘い の4行を落とした。ユーザー判断＝**一旦これで様子を見たい**（ことわざ好きは残す・頻度は「多い」→「たまにある」へ）。
- 理由の言語化はされていない＝**推測で理由を書き足さない**。戻したくなったら git から拾える。
- `summonLines.ts`（AI が出せなかったときの召喚の受け）は定型4行に依存していないので無改修。

### 窯の待ちセリフに残っていた「レア」を削除
`waitLines.ts` の「レアなアイテム同士だと、すごいのができやすいんだって」＝**レア度を全廃（2026-07-05）した後の残骸**が窯の待ち画面で出続けていた。削除。
- ⚠️ **AI を通らない固定セリフ（`waitLines`／`failureLines`／`summonLines`／オンボ台本）は仕様変更に自動では追従しない。** 仕様から概念を消したら、この4ファイルを grep する。

---

## 2026-08-17

### ホームの「直前のこちらの発話」は、大セリフとひと続きのときだけ出す
ユーザーFB「**前のこちらの入力テキストが時間を空けても残り続けている。コレットは時間をあけると挨拶するけど、こちらの前のセリフだけ見えてて変**」。
- 原因＝`HomeMode` の `prevUser` が「大セリフの直前にあるユーザー発話」を**無条件に**拾っていた。間が空くと第一声（`openConversation`）が履歴の末尾に足されるので、**大セリフだけが新しくなり、その直前＝前回の自分の発話が残る**。会話としては**こちら側だけ過去のまま**並ぶ。
- 対処＝**隣接（大セリフの直前）＋間隔 5分以内**のときだけ出す。送信中は返事がまだ無いので基準を現在時刻にして「いま送った文」を見せる。
- **測るのは「いまから何分前か」ではなく、発話と大セリフの間隔。** 前者にすると、20分ぶりに開いて第一声が出ない（`REUNION_QUIET_MIN` 未満＝会話の続き扱い）ときに、**対だったやりとりの片方だけが消える**。
- ⚠️ 副次的に、カメラ/召喚のひとことが大セリフのときも古いユーザー発話が並ばなくなった（隣接条件で外れる）。こちらは元から噛み合っていなかった。

### 立ち絵のリアクションと掛け声は「返事1件につき1回」＝ホームに戻るたび鳴らさない
ユーザーFB「**たからばこやずかんから戻ったときにいちいち前のリアクションが再生されて変。カメラからホームに行った時もまあまあ変**」。
- 原因＝`App.tsx` は `screen === 'home' && <HomeMode />` なので**画面を移るとホームはアンマウントされる**。一方トリガーの `replyNonce` は chatStore 側＝残る。**再マウントのたびに同じ nonce で effect が走り**、直前の返事のアニメと掛け声をもう一度再生していた。
- ⚠️ **`useRef` では直せない**（マウントごとに初期化される）。**アンマウントを跨ぐ状態はストアに置く**＝`chatStore.reactedNonce`＋`markReacted()`（永続はしない＝リロードで 0 に戻ってよい。だから**リロード後だけは鳴らない**＝症状が間欠に見えていた）。
- 「もう再生したか」は**表示ではなく会話の状態**なので、置き場としても component 側より store 側が正しい。

### 自動再生のアンロックは「特定のボタン」ではなく「最初の操作なら何でも」
ユーザーFB「**なにかするまで音声が鳴らないのが気になる**」。
- **前提＝読み込み直後に鳴らすことは原理的にできない。** ブラウザは操作なしの音声再生を許さない。**やれるのは「最初の操作を取りこぼさない」ことだけ**なので、直せるのは*どれだけ待たされるか*であって*待つかどうか*ではない。
- **実際の穴＝`primeAudio()` が4か所のボタンにしか付いていなかった**（撮影・チャット送信・声トグル・オンボの「はい」）。つまり**図鑑やたからばこを開いて眺めるだけの人は、BGM が最後まで鳴り出さない**。声トグルの呼び出しは `if (!voiceEnabled)` なので、既定 ON の人には**そもそも発火しない**。
- 対処＝`installAudioUnlock()`（`App.tsx` でマウント時1回・document の **capture 段**で `pointerdown`/`touchend`/`keydown` を一度だけ拾う）。capture なので途中で `stopPropagation()` するUIがあっても効く。
- **各ボタンの `primeAudio()` は消さない**＝そちらの方が早く鳴る場面があり（撮影の判定音など）、`primed` フラグで冪等なので二重には効かない。**「保険を足す」であって「置き換える」ではない。**
- ⚠️ **ボタンごとにアンロックを付ける方式は、ボタンが増えるたび付け忘れが増える。** 今回の穴もそれ。横断的な関心事は横断的な場所（document）で1回だけ処理する。
- **残る体験の穴**＝それでも「最初の1タップまでは無音」。**ここは Web では埋まらないので埋めにいかない**（下記）。

### 「最初の1タップまで無音」はネイティブ化（N-1）で消える＝いま起動画面を足さない
ユーザー質問「これはブラウザの仕様？アプリでは初めから鳴るよね」＝**どちらもそのとおり**。
- **これは autoplay policy**（user activation requirement）＝ブラウザ側の規約で、アプリの実装では外せない。**ミュートなら自動再生は通る**（規制対象は「音が出ること」）。**ホーム画面に追加した PWA でも外れない**＝「アプリっぽくしたのに鳴らない」となる。
- ⚠️ **Chrome デスクトップの Media Engagement Index に注意**＝よく音を鳴らすサイトだと学習して自動再生を許すようになる。**開発機だけある日から鳴り出して、実機で再現しない**という形で混乱する。iOS Safari は最も厳しく、しかも **`<audio>` 要素ごと**にアンロックが要る（声用と BGM 用で `primeAudio()`/`primeBgm()` が別々に要るのはこのため）。
- **ネイティブの WebView はこの制限をオフにできる**（Android＝`setMediaPlaybackRequiresUserGesture(false)`／iOS WKWebView＝`mediaTypesRequiringUserActionForPlayback` を空に）。Capacitor は生成された native プロジェクトを触れるので、**N-1 の時点でこの穴は消える**。※実際の配線箇所は N-1 着手時に現物で確認する。
- **したがって「タップしてはじめる」起動画面は作らない**＝**ネイティブ化で消える制約のために、オンボを終えた人が毎回1枚踏む画面を足すことになる**。割に合わない。Web としては `installAudioUnlock()` でほぼ上限。

---

## 2026-08-15

### `dvh` ズームを採っている以上、「固定幅 × `flex-1`」は画面が高いほど壊れる
症状＝**iPhone 14 Pro で会話の送信ボタンが画面外に消えていた**（Android では出ない）。原因＝入力欄が `flex-1` なのに **`min-w-0` が無い**。flex アイテムは既定で `min-width:auto` ＝ **中身の固有幅より小さくならず**、`<input>` の固有幅（約20文字ぶん）は**ルート font-size に比例して広がる**。
- **`html { font-size: clamp(16px, 100dvh/46, 20px) }` で UI 全体をズームする設計なので、背の高い画面ほど固有幅が広がって行があふれ、`shrink-0` の隣（送信ボタン）が押し出される。** ルートが `overflow-hidden` なのでスクロールもできず、**ただ消える**。
- **iOS 固有ではない。画面の高さの問題。** Android で出なかったのは `dvh` が小さくて font-size が下限 16px に張り付いていたから。**手元の端末で出ない＝直っている、ではない。**
- **`<input>` だけが特別**＝折り返せず固有幅も大きい。テキスト要素（`WorkingScreen` のタイトル、オンボの選択肢）は折り返せるので固有幅がほぼ1文字ぶんまで縮み、同じ問題は起きない。
- **横に並べるときの規則＝`flex-1` 側に `min-w-0` を付ける。** UI-NOTES の「px 固定の寸法を足すと端末ごとに比率が崩れる」と同じ根＝**ズームする設計では、スケールしない寸法とスケールする寸法を隣に置くと必ずどこかで衝突する。**

### 実機で確認：BGM は「掛け声が機械的」を解消した
ユーザー評価＝「**BGMがあるといっきにちゃんとしたアプリ感が出る。パートボイスの無機質な感じも気にならなくなってきた**」。
- **掛け声の"機械的さ"は、掛け声側の問題ではなく床が無かったこと**だと確定した。実装当日に検討していた対策（**掛け声を6割の確率に間引く／`neutral` の本数を増やす**）は、**当面不要**。
- ⚠️ **飽きだけは数日使わないと出ない**（毎日開くアプリなので、メロディのある曲は繰り返しに弱い）。ここだけ様子見を続ける。

### BGM を入れる ── 声が寂しく聞こえたのは「音の床」が無かったから
ユーザーFB＝「**ボイスだけ響くのが寂しい。ホームは画面に注目が集まるからかな**」。診断＝**完全な無音の上に声だけが乗ると、「人の声」でなく「システム音」に聞こえる。** ゲームでもVNでも声が無音に単独で乗ることはなく、必ず音楽/環境音の層がある。床があると声はその空間の中の出来事になり、無いと通知音と同じカテゴリに落ちる。**掛け声（Ⅰ-6b）が「機械的」に感じられたのは、文面や演技ではなくこれが原因だった可能性が高い**＝順序として BGM が先に必要だった。
- **ダッキングとセットで初めて成立する**（声のあいだ 0.35→0.12）。BGM を足すだけだと、そのぶん声が埋もれる。
- **決定**＝鳴らすのはホーム系・たからばこ・オンボ。**カメラは鳴らさない**（屋外＝現実の音が既に鳴っている）。**ゲームは専用曲を置いたときだけ**（ホームのヒーリング系は合わない）。ON/OFF は**声のトグルに相乗り**（設定を増やさない）・既定 ON。
- **ファイル名＝シーン名**にして、置くだけで有効・無ければ黙る形にした（スプライト・掛け声と同じ流儀）。ゲーム用を後から足すのにコード改修が要らない。
- **同じ曲なら鳴らし直さない**＝画面を移るたびに頭から再生すると「別の場所に来た」感じが出すぎる。
- 素材は**環境音寄りを推したが、ユーザーは Pixabay と甘茶の音楽工房から音楽を採用**（クレジット不要）。毎日開くアプリなのでメロディの飽きは要観察。

### BGM は 96kbps に落とす（`npm run bgm:optimize`）
配布サイトの mp3 は 256〜320kbps で来るが、**音量 0.35 で声の下に敷く音**にその品質は要らない。実測で **8.2MB → 3.6MB**（home 6.43→2.41／game 1.47→1.10）。効くのは**スマホの初回ダウンロード**＝屋外で開くアプリなので、鳴り出すまでの待ちがそのまま体験に出る。
- Pixabay は品質を選べず、甘茶の音楽工房も同様だったので**落とし直しでは解決しない**＝スクリプト側で吸収した（`ffmpeg-static` を devDependency に追加・ビルド時のツールなのでアプリには同梱されない）。
- **冪等**＝先頭フレームのビットレートを読んで、既に目標以下なら触らない。出力は CBR なので2回目以降は必ずスキップになる。
- ⚠️ **先頭フレームだけ見た長さの推定は VBR で外れる**（`game.mp3` を40秒と読んだが実際は約1分35秒）。長さを測りたいときは信用しないこと。

### ショップ（機能アイテム解禁）は作らない ── 社内公開には過剰
2026-07-16 に決めた「機能をアイテムとして買って解禁する」（spec §4.8）を**段から外してバックログへ**。**却下ではなく、いま建てるものではないという判断。**
- **釣り合っていない**＝解禁対象は**3つだけ**（窯・タワー・フラッピー。アルバムは元から対象外＝贈り物）。しかも現状メニューは**4行**で、解禁ガードは1つも無い。**4行を段階的に出すために、通貨・ショップUI・購入フロー・購入演出・おねだり規律を作る**ことになる。
- **売り切ると空になる**＝3つ買われたらショップは永久に空。1週間で死ぬUIが1画面残る。
- **上流が2つ詰まっている**＝通貨には「貯める理由」が要るが、**長期の目標が未定**（§16）。バランスを決める基準が無い。
- **本来の目的は「段階的開放」であってショップではない。** それだけなら**コレットからの贈り物**（§4.8 でアルバムに使っている方式）で足りる。通貨も店も不要で、**「買った」より「もらった」のほうが関係性の製品に合う**（「課金で関係を人質にしない」§9 とも整合）。
- **戻すトリガー**＝解禁対象になる機能が増えたとき（マップ＝「妖精の地図」等）、またはマネタイズ（ROADMAP 8）に着手するとき。**spec §4.8 は構想として残す**（消さない）。

---

## 2026-08-14

### ホームの会話の声は「感情ごとの短い掛け声をランダム」にする
返事は AI の自由文なので事前収録できない。**声＝反応／文字＝内容と役割を分ける**ことで解いた＝返事に付いてくる `emotion` で `voice/<感情>/` から掛け声（「わぁ！」「そっかあ」）を1本鳴らす。何を返しても矛盾せず、実行時ゼロ円で生成待ちも無い。
- **立ち絵の `sprites/<感情>/` と同じ仕組みに乗せた**＝ファイルを足すだけで候補が増え、直前と同じものは避ける。新しく作った概念はゼロ。
- **短い掛け声に限る**（決め台詞クラスの長さにしない）＝長いほど「画面の返事と別のことを言っている」のが目立つ。
- 収録が無い感情は**黙る**（動的TTS に落とさない）。ホームで声にお金をかけない、が方針の芯なので、フォールバックでそれを崩さない。
- **🔊 タップは残す**＝押した回だけ全文を動的TTS で読む。普段は無料のまま「全部読んでほしい」に応えられる。
- **文字は画面に出ないので、台本のような一致チェックは要らない**（違うことを喋る事故が原理的に起きない）。だから**ファイル名も本数も自由**にでき、クライアントはフォルダの中身を拾うだけでよい。

### 掛け声は自動収録をやめ、手で作って置く運用にした
初回に自動収録した42本を聴いて「合っていない・ノイズが入っている」ものを外した結果、**掛け声は当たり外れが大きく、都度選ぶ必要がある**と分かった（2026-08-14）。**`voiceLines.ts` の `REACTION_LINES` を空にして、掛け声は `voice/<感情>/` に手で置く。** 文面を書けば自動収録は復活する（両対応）。
- ⚠️ **このとき収録スクリプトの後片付けが凶器になる**＝当初の実装は「感情ごと消えたらフォルダの中身を readdir して全消し」だったので、**手で置いた音声を巻き込んで消す**。**消してよいのは manifest に載っている＝スクリプト自身が録ったものだけ**に直した。フォルダも空になったときだけ消す。
- 検算＝`npm run voice:record` を回して「収録 0 / 変更なし 7」かつ**mp3 の md5 が1本も変わらない**こと。

### Fish は間欠的に「生成が止まらない」＝収録スクリプトで長さを検査する
42本を収録したとき、**3本が約47秒（761,938 bytes）で返ってきた**（「えへへ」「うぅ…」＝短い掛け声）。**異なる文面でバイト数が完全に一致する**のが手がかりで、上限まで走り切ったときの固定長だった。
- **文面のせいではない**＝同じ「えへへ」を単体で録り直すと 0.6 秒で正常に録れた。**間欠的な失敗**なので、文面を変えて回避しようとすると原因を見失う。
- **対処＝`synthesizeChecked`（文字数から妥当な上限を見積もって、超えたら最大3回まで録り直す）。** 黙って書くと 700KB 超のゴミがコミットされ、再生すると延々鳴る。**それでも駄目なら例外で止める**（壊れた音声を成功として通さない）。

### パートボイスは「収録した台本」を manifest に控えて突き合わせる
固定セリフを事前収録すると、**台本を直したのに録り直していない**状態が必ず生まれる（`script.ts` は非コーダーが直接編集する前提のファイル）。そのとき古い音声を鳴らすと**画面の文字と違うことを喋る**＝いちばん悪い壊れ方になる。
- **決定＝`manifest.json` に収録時の `text`/`expression`/`direction` を控え、再生前に今の台本と全一致するかを見る。** 一致しなければ黙って動的TTS に落ちる（声は1〜3秒遅れるが、**必ず今の文面を喋る**）。
- **同じ判定を収録スクリプト側でも使う**＝変更が無い回は Fish を叩かない（冪等・無料）。「録り直しが要るか」と「再生してよいか」が同じ1つの条件になる。
- 収録の名簿は**台本ファイル自身**（`SPOKEN_FIXED_LINES`）＝コピーを作らない。スクリプトは esbuild で `script.ts` を評価して読む（このリポジトリで実績のある手口）。
- **読み方は実行時（`api/tts.ts`）と同じルールに揃える**（演技指示を角括弧で前置／無ければ `expressionTag`）。ここがズレると収録した分だけ声の雰囲気が違う。ただし `latency:'low'` は付けない＝収録は待てるので品質優先。

### 収録済みの mp3 は「選んだテイク」なので `--force` で潰さない
Fish は同じ文面・同じ演技指示でも毎回わずかに読みが揺れる（イントネーション・ノイズの乗り方）。**いま入っている7本は複数テイクを聴き比べて選んだもの**で、`intro-1`/`intro-2`/`intro-3` は実際に録り直して差し替えている（intro-1・intro-3＝ノイズ、intro-2＝語尾）。
- **`npm run voice:record --force` は全部を生成し直す＝選んだテイクが消える。** 使わない。台本を直したときの通常実行（差分のある分だけ録る）で足りる。
- **テイクを選び直したいときの手順**＝候補を scratchpad に録って聴き比べ → 当たりを `src/characters/<id>/voice/<lineId>.mp3` に上書きコピー → 文面を変えたなら `manifest.json` の当該エントリも合わせる → `npm run voice:record` が `変更なし` と言えば整合が取れている（**言わなければ食い違っていて、そのまま上書きされる**＝これが検算になる）。
- ⚠️ **`manifest.json` は生成物なので普段は手で触らない。** 上の手順のときだけ例外的に触る。ここだけ新しくして mp3 が古いままだと、突き合わせが通ってしまい**画面の文字と違うことを喋る**。

### 読み上げない固定セリフは収録しない
`CHAT_COACH_LINE`（第一声と被るので読み上げない）・`SUMMON_COACH_NOTE`／`CAMERA_HINT_TEXT`（コレットでなく画面の声）は対象外。**録っても再生される場所が無い。** 収録対象を「台本にある全部」でなく「実際に `speakLine` する分」と定義した。

---

## 2026-08-12

### 図鑑の再発見が iPhone で落ちていた＝**IndexedDB から読んだ Blob は書き戻せない**
症状＝「カメラで図鑑にあるものを撮って『そのまま』を押すと、図鑑が写真無し扱いになり、その後同じものを撮っても登録されない」。実機で出た例外は **`UnknownError: Error preparing Blob/File data to be stored in object store`**。
- **原因**＝WebKit では `getAll()` 等で取り出した Blob は**ファイルへの参照**で中身を持たない。それをそのまま `put` すると書き込みの準備段階で落ちる。図鑑の再発見は `{ ...existing, count+1 }` を書くので、**アプリ中で唯一「読んだ Blob を書き戻す」経路**だった（新種は作りたての Blob を書くので落ちない）。
- ⚠️ **一般化＝IndexedDB には部分更新が無い。** スカラー1つ直すだけでもレコード全体、つまり Blob ごと書き直す。**「Blob は触っていないから安全」は成り立たない。** 読み出したレコードを書き戻すときは必ず `lib/storage/blob.ts` の `rematerializeBlob` を通す。
- **「そのまま」ボタンは無実だった**＝あれは state を消すだけで何も書かない。壊れていたのは押す前の `collect()` で、ボタンは気づくタイミングでしかなかった。**症状の出た操作と原因の場所がずれる典型。**
- 読み出せない Blob だったときは `null` を返し、**いま撮ったクロップで埋める**（記録ごと失うより良い＝壊れたエントリの自己修復も兼ねる）。
- **見つけられた理由＝握りつぶしをやめたから。** `CameraMode` の収集は `catch {}` で例外を捨てており、**エラーも出ず図鑑にも入らない**状態だった。console にも出ないので外から切り分けられない。**「失敗を黙って諦める」catch は、演出の話であっても例外だけは必ず残す。**
- 実機（iPhone Safari）は console が見られないので、`?debug=1` のときだけ画面に出す診断（書き込み直後に読み戻して Blob のサイズと実読を表示）を足して1回の再現で特定した。

### 召喚後のひとことは読み上げない（保留にしていた判断を決着）
Ⅰ-5b の実装時に「イベント的な一言（カメラ・オンボ・コーチ・たからばこ初訪問）は自動で喋るのに、召喚のひとことだけ黙っているのは筋が通るか」を保留にしていた。**実機を見たユーザーの判断＝読み上げない**。
- 実装は既定（ホームの大セリフは自動読み上げしない）のままなので**コード変更なし**。喋らせたくなったら `speak()` を1行足すだけ。
- これで Ⅰ-5／Ⅰ-5b／Ⅰ-5c の宿題はすべて閉じた。

### 召喚専用の立ち絵 `casting` を入れた（マゼンタ抜きの品質は数字で判定）
待ち画面に出るコレットを `searching` から **`casting`（両手を広げて掲げた＝魔法をかけている姿）** に差し替え。**表示専用のポーズ**なので `FAIRY_EXPRESSIONS` には足すが `CHAT_EMOTIONS`（AI が選ぶ側）には入れない＝`salute`・`searching` と同じ扱い。**召喚も窯も同じポーズ**（どちらも魔法をかけている。分けたくなったら `context` で出し分けられる）。
- 素材はマゼンタ地（`#FF00FF`）で描いてユーザーが自分で抜いた。**目視では判断できないので数えた**：残留マゼンタは**不透明 4px のみ**／半透明の縁に 7927px（**平均 alpha 39/255**）／キャラ内側の"穴" 7095px は**全部が正しい透過**（上位3つは上げた腕・肩・髪に囲まれた閉領域）。
- **フチのマゼンタは実在する**＝原寸を白地に置いて3倍に拡大すると、翅の外縁に 1px のピンクの線が見える。**ただし表示は 176px＝1254 から 7分の1 に縮むのでその線は 0.14px になり、実背景・実寸の合成では見えない。** ユーザー基準（「ぱっと見で分からなければいい」）で合格とし、despill はかけていない。
- **framing は既存と揃っている**＝内容の bbox が縦 94%（既存は 95〜97%）。腕を上げても**手の先がリボンとほぼ同じ高さ**で止まっているので頭が下がらず、他の表情と並べても身長が変わらない。**腕を上げたポーズを足すときはここを見る。**
- ⚠️ **1254px の PNG（1.4MB）で置かれていた**ので `npm run sprites:optimize` で WebP 1024px 化（126KB）。他のスプライト（115〜122KB）と同水準。

### 完成後の「出現の演出」は作ったうえで不採用（`SummonReveal` を削除）
待ち画面（`GeneratingOverlay`）を魔法陣＋"溜め"に作り替えた結果、その直後に光が集まって弾ける 1.5 秒（`SummonReveal`）を再生すると、**同じ絵を2回見せる**形になった。ユーザーの言葉＝**「魔法陣がある今となってはいらないな」**。
- **溜めの payoff は「アイテムそのものが出ること」で足りる**＝生成完了 → 結果カード（`animate-reveal` の 0.5 秒ポップイン）に直結させた。フェーズから `revealing` を落とし、コンポーネントと `tailwind.config.js` の `summonGather`／`Flash`／`Ring`／`Emerge`／`Spark` も削除（`summon-pull`・`spin-slow` は待ち画面が使うので残す）。
- ⚠️ **順番の教訓**＝「完成の瞬間を派手にする」より先に**待たされている ~7 秒**を作り替えるべきだった。先に花火（Ⅰ-5）を作ったので、後から本体（待ち画面）が出来たときに花火が余った。**体験の重心は"待ち"にある**（1.5 秒 vs ~7 秒）。
- 消したコードは git 履歴に残っている（`31d83aa` 時点）。同じものが要るなら拾い直せる。

### `?debug=1` の「演出を見る」系ボタンを全廃
「出現の演出を見る」「生成中の演出を見る」を削除。**元の理由は「演出を1回見るのに ¥7 かかるから」**だったが、ユーザー判断＝**「たいした金額でもない」**。
- 検証用の分岐が本番のコードに残り続けるコスト（読む人が増える・実物とズレる）の方が高いという整理。**演出の確認は実召喚で行う。**

### 召喚の締めを「ホームに戻ってコレットがひとこと」にする（Ⅰ-5b）
**きっかけ＝ユーザーの違和感**「召喚してすぐ宝箱行きなのが、作業感があるのかも？」。出現の演出（Ⅰ-5）まで作り込んでも、その後が結果カードで終わると「在庫が1つ増えた通知」になり、前半の重さに後半が負ける。
- **決めたこと**＝結果カードは残し、**ボタンを「OK」1つ**にして、押したら**ホームへ戻りコレットがそのアイテムにひとこと言う**。そのセリフが大セリフである間だけ**立ち絵の横に透過アイテムが浮かぶ**。召喚を「獲得」ではなく「きみが見つけたものを渡した」で終わらせる。
- **専用の対面画面は作らない。** `chatStore.appendSummonLine` でセリフを会話履歴に積んで `go('home')` するだけで、**大セリフ・表情・立ち絵のリアクションが全部ただで乗る**（カメラの `appendCameraLine` と同じ流儀）。※前段の試作では専用オーバーレイ（`SummonMeeting`）を作ったが、ユーザーの指示は「ホーム画面に戻ってコメント」だったので不採用。
- **アイテムの表示は `ChatMessage.itemId` に紐づける**（`photoId`/`entryId` と同じ形）。**セリフに紐づくので次の発言で自然に消える**＝「いつ消すか」の状態を持たなくていい。ここが専用 UI を作らずに済んだ理由。
- **ひとことはメタ生成（`/api/generate-item`）に相乗り**＝`comment` を responseSchema に1フィールド足しただけなので **API 呼び出しもコストも増えない**。会話 API を別に叩く案は採らない（1回 ¥7 の行為に更にレイテンシと費用を足す理由がない）。
- **説明文の役割を分けた**＝`description` は**コレットのセリフをやめてコミカルな説明文へ**（「じゃがいもを油で揚げた料理。ホクホク、ジューシーでたまらない！」）。コレットが喋る分は `comment` に移ったので、たからばこの詳細で二重にセリフが出なくなる。**窯（合成）も同じ規則を共有**＝たからばこの詳細は召喚品と合成品が同じ欄に出るため（ユーザー判断で揃えた）。
- ⚠️ **説明文をコミカルにすると安全側のリスクが生まれる**＝「たまらない！」のような食欲を煽る書き方が、きのこ・野草のような誤同定の危険がある対象にも付きうる。図鑑の解説には既にあった制限を `DESCRIPTION_RULES` にも入れた（食用可否・毒性・薬効を断定しない／おいしそうに書かない）。
- **読み上げはしない**＝調べたところ**ホームの通常の返事は自動読み上げされない**（ログの🔊ボタンのみ。自動で喋るのはカメラ・オンボ・コーチ・たからばこ初訪問といったイベント的な一言だけ）。召喚のひとことは大セリフに乗るので、既定に合わせて黙って出す。**イベント扱いにして喋らせるかは実機で判断する**（TTS は1回 ¥0.3 程度なのでコストは論点ではない）。
- **実装の罠**＝ホームでアイテムが見つからないとき（召喚後にリロードした経路）に永続層を読み直すが、**条件だけで弾くと無限ループする**（アイテムが削除済みだと読み直しても見つからず、status が idle に戻るたび再発火）。ref でマウントにつき1回に固定した。

### 「これの話をする」の返事が記憶に流れていた（バグ）
アルバム/図鑑から振った話題は `origin` 付きで記憶要約の対象外にしている（アプリ由来の発言を機械的に蒸し返さないため）。**ところがコレットの返事にだけ `origin` が付いておらず**、きっかけのユーザー発言だけが filter で外れて**返事だけが記憶に流れる**状態だった（同じ関数の「材料ゼロ」経路では正しく渡していたので、単なる書き漏れ）。写真を見た返事は推測を含みうるので、それだけを記憶に残すと**事実でない思い出**になる。1行修正。
- 外部レビュー（ChatGPT にリポジトリを読ませたもの）由来の指摘で、**実際にコードを読んだうえで当たっていた唯一の項目**。他（api の無認証・テスト0）は `http.ts` の冒頭コメントと ROADMAP に自分で書いてある内容の読み上げだった。

## 2026-08-09（続き）

### 大セリフの下端は「溶かして切る」＝ただしフェード幅と下パディングは対で動く
5行を超えた大セリフが直線でスパッと切れ、**白い滲みごと四角く切り落とされる／続きがあることに気づけない**（実機FB）。
- **効いた理解＝あの白さの主成分は文字のハロー（`HERO_TEXT_HALO`）**。だから**文字を溶かせば白も一緒に溶ける**。後ろの雲（`HERO_CLOUD`）は別レイヤーなので滲みは保たれる。マスクは `-webkit-` 併記で、効かない環境でも「今までどおり直線で切れる」だけに劣化する。
- ⚠️ **フェード幅と下パディングは必ず同じ値にする**＝最下部まで送ったとき最終行がフェード帯の上に収まらないと、読み終わりが薄いままになる。**この対のせいで「フェードを深くする＝短いセリフの下が広がる」という直接のトレードオフになる**（実際、最初に入れた `pb-6` が3行のセリフの下を広げてユーザーに指摘された）。現在は 0.75rem で揃えている。
- **立ち絵との間隔はほぼこの下パディングだけで決まる**＝スプライトの透明余白を sharp で全枚数実測したところ**上は 0〜2%**（176px 表示で3px以下）で、絵のせいではなかった。
- **比較モックで分かったこと**＝`pb` の 0.5〜0.75rem の差は並べても判別しにくい。**ここは大きくは動かせない**ので、さらに詰めたいなら「スクロールできるときだけフェードと余白を出す」（`onScroll` で状態を持つ）しかない＝未着手。

### 「図鑑にコレットを住まわせる」は不採用（前提が間違っていた）
ROADMAP に「いまの図鑑は無人の棚」と書いていたが、**事実誤認**。`WorkingScreen`（図鑑・アルバム・窯・たからばこの共通シェル）が**すでに右下にコレットを置いており**（144px・`useFairyReaction` でリアクションも出る）、隠れないよう `pb-44`（≒200px）の余白まで確保している。つまり「住まわせる」は実装済みで、残るのは装飾だけだった。
- **同日に Ⅰ-4c（図鑑の「これの話をする」）を入れたことで、必要性がさらに落ちた**＝この項目の裏にあった「図鑑を見ているとき何か言ってほしい」は、**ユーザー起点・押したときだけ・ちゃんとした会話**として満たされた。ここでコレットが勝手に喋る形を足すのは、同日に避け続けた「毎回機械的に言及する」を自分から作りに行くことになる。
- ユーザーの見立て＝「単純に浮いてるのはかわいいだけで邪魔な可能性がある」。**追加ではなく削減side を疑うなら `pb-44` の方**（毎画面で 200px を右下コレットのために払っている）。いまは問題として挙がっていないので触らない。

### ホームのコレットは大きくしない（UI-NOTES #1 を不採用でクローズ）
「`lg`=176px は小さめ、画面高の半分前後まで大きくしては」という宿題を、実機を見たユーザーが**「そもそもいらない。今の大きさでちょうどいい」**と判断。数値をいじらずに閉じた。UI-NOTES から削除。

### カメラの発言を履歴に積むとき、**機械的な蒸し返しは「送信窓」では防げない**（記憶ファクト経由で漏れる）
Ⅰ-2 の設計中、ユーザーの「毎回前回撮ったものに言及するのは機械的だからやめてほしい」を受けて配線を追ったら、**当初の説明（12件の送信窓から押し出されて消えるから大丈夫）が成立していなかった**。
- **実際の経路**＝履歴は窓から出る前に `CONSOLIDATE_EVERY=6` で**記憶ファクトへ要約**され、`gatherChatContext` がそのファクトを**毎回のプロンプトに載せている**。つまり「イチョウを見せてもらった」がファクト化されると、窓とは無関係に**以後ずっと蒸し返される**。
- **対策＝`ChatMessage.origin='camera'` を要約対象から外す**。ただし「要約済み」カウンタは前進させる（進めないと切り詰めが走らず履歴が伸び続ける）。
- ⚠️ **一般化＝毎回プロンプトに載るもの（接地ノート・記憶ファクト）に入れた情報は、必ず毎回言及される。**「たまに触れてほしい」ものは**履歴として流す**しかない。**"今日の写真"のような欄を作って毎回渡す設計は、いちばんやってはいけない形。**
- 触れすぎ防止は**二段**＝①要約から外す（構造）②`persona.md`「前に見せてもらったものに毎回は触れない」＋第一声プロンプトの同趣旨ガード（文言）。時間帯の接地で効いている「さりげなく反映・毎回は言わない」と同じ流儀。
- **実 Gemini のプローブで確認**＝撮影後に無関係な会話を5ターンで**写真への言及 0/5**／「さっき見せたやつ、なんだっけ？」には**イチョウと正しく答える**。ただし「今日は外がきれいだったな」では触れず＝**やや控えめに振れている可能性**があり、実機の体感で文言を弱める余地を残す。
- **副作用（意図せず構造上こうなった）**＝撮った直後にホームを開くと**第一声が出ない**。`reunionBucket` が「最後の発話からの経過時間」で判定するため、カメラの発言が最後の発話になり 30分以内＝会話の続き扱いになる。挨拶し直さないので自然と判断して残した。
- **検証の手口（再掲・有効）**＝`api/_lib` を esbuild でバンドルして node から実 Gemini を叩くプローブ。dev サーバもブラウザも要らず、system prompt の変更を直接測れる。

---

## 2026-08-08

### ROADMAP を4段（芯→提出→深さ→土台とリズム）に組み替え、**STEP6 と STEP N を1つの塊にした**
「土台（Supabase）を先にやる」という並びを**やめた**。きっかけ＝ユーザーの「Supabase移行はそもそもなんでやろうとしてたんだっけ」。理由を洗い直したら、**当初の位置づけ（＝土台だから先）が読み違えだった**と分かった。
- **STEP6 の価値5つのうち4つは「提出後」にしか発生しない**＝①端末をまたぐ引き継ぎ ②プッシュ通知の前提 ③マネタイズの前提 ④誰の呼び出しか分かる（コスト管理） ⑤pgvector。②③④は提出後、①も1端末では痛みが出ない。
- **本当の理由は「通知」だった。** Capacitor の**ローカル通知だけならサーバは不要**だが、それは*タイマーであってコレットではない*。「先週きみが撮ったあじさい、そろそろ枯れちゃうかも」を送るには**送信の瞬間にサーバが図鑑を読んで LLM を叩く**必要があり、端末は寝ているので端末では作れない。**このアプリで最も価値のある一撃が、サーバに関係データが無いと物理的に作れない。** → だから STEP6 と STEP N は**分けない**。分けると「画面が変わらない土台工事」、セットなら「コレットの側から話しかけてくる」になる。
- **順番を最後にする理由＝スキーマは体験が確定させる。** Ⅰ-2（カメラの会話を `chatStore` へ）を入れると `conversations` に「発言の場所・紐づく写真」が要ると分かり、エピソード要約を入れると `memories.kind` が変わる。**先に移行すると確実に2回移行する。** spec §6 のテーブルが「（案）」のままなのは正しい。
- ⚠️ **中途半端な STEP6 が最悪**＝「認証はあるが写真はローカル、エクスポートは無い、テストも無い」は*土台に手を付けて終わっていない*状態。対して **local-first をやり切れば設計の主張になる**（spec §9「全員から強制的に PII を集めない」）。
- **ただしエクスポートは前倒し（Ⅰ.5-3）**＝データ保全は Supabase 無しでも要る。**エクスポートは"保険"に過ぎない**（ユーザーに操作を要求する）ので、STEP6 の「記憶を守る」を代替はしない。**「記憶を人質にしない」と「記憶を守る」は別**という整理。

### 「マイプロダクト」提出の観点で効くのは Supabase ではなく README・api ガード・テスト
審査で見られる経路は3つ（①アプリを触る ②リポジトリを見る ③技術的に突かれる）。それぞれの穴を測った結果、**同じ2日なら STEP6a より下の4つが効く**と判断した。
- **②が最大のレバレッジ**＝spec 50KB・**DECISIONS 33KB**・ROADMAP・UI-NOTES・CLAUDE.md という判断の蓄積は個人プロダクトにまず無い強み。**なのに README が存在せず、入口が無いので読まれずに死んでいた。**
- **③の穴は3つだけ**＝**テスト0件**（言い訳しにくい減点）／**`api/` が無認証**（¥7/回が誰でも叩ける。DECISIONS に「承知のうえ」と書いてあっても**外からは「意図した」と「気づいていない」の区別が付かない**）／データが端末ローカルのみ。抽象化（Provider 6種・Repository・CharacterRenderer）と鍵のサーバ集約・`http.ts` の入力ガードは**すでにある**。
- **①で最優先は 3b（パートボイス）**＝オンボは*審査員が最初に触る10秒*。文字が先に出て声が後から追いつく状態で出さない。

### 新しい感情の追加コストを実測＝**5か所・1時間**（安い）
「眠い・ドキドキ・得意げ・心配」を足したいという要望を受けて配線を調べた。サーバ側の感情リストは `CHAT_EMOTIONS` **1本**が chat / identify / scene の**3スキーマ全部に配られている**ので二重管理が無い。触るのは `FAIRY_EXPRESSIONS`／`CHAT_EMOTIONS`／`persona.md` の感情一覧／`voice.json` の `expressionTag`／`REACTION_ANIMATION` の5つ。**絵が無い間は自動で neutral に落ちるので、enum だけ先に入れても壊れない。**
- **⚠️ 眠いは「反応」ではなく「常態」**＝他の感情は*ユーザーの発言への反応*だが、眠さは**時間帯で決まる**。`CHAT_EMOTIONS` に入れると Gemini が会話内容から眠さを選んで変な出方をする。→ `HomeMode` の `baseExpression` 側に `timeOfDayLabel` の**深夜**を繋ぐのが正しい配線。
- **⚠️ 意味が被った感情を足すと、AI がどちらも選べず両方出なくなる**。「ドキドキ」が期待寄りなら既存 `excited` と食い合うため命名を保留（緊張寄りなら `nervous` として独立の価値あり）。`proud`（得意げ）・`worried`（心配）は被らないので即足せる。
- **実態＝12感情に対し画像は16枚**。2枚以上あるのは confused/sad/salute/surprised/thinking の5つだけで、**7感情は1枚きり＝毎回同じ絵**。`lv1/lv2` の好感度別フォルダは**1つも作られていない**（`Sprite2DRenderer` は対応済みなのに）。**コードは完成していて、足りないのは絵だけ。**

### 溜まっていた実機目視をまとめて消化（4件すべて「OK」でクローズ）
UI-NOTES に「残＝実機」で積まれていた4件を実機で確認し、**いずれも追加調整なしで決着**（ユーザー判断）。UI-NOTES からは落とした。
- **図鑑の標本カード**（紙 `#F3E9D2`／ノイズ 0.16・140px タイル／明朝 Shippori Mincho の可読性／テープ／2列／空きマスの「未発見」／判子）＝狙いどおり。08-04〜08-05 の「質感は知覚できる強さまで上げる」の結論が実機でも保たれた。
- **失敗時のコレットのセリフ**（機内モード）＝OK。
- **ホーム背景の夕/夜**（半透過後の可読性）＝OK。朝で確認済だった①〜③が他の時間帯でも崩れない。
- **たからばこの手触り**（浮遊感／漂いの速さ `DRIFT_MIN_S`〜`DRIFT_MAX_S`＝6〜11s／振れ幅／掴んだときのスナップ）＝OK。**チューニングの宿題（v2-8）はチューニングせずに閉じた**。
- ⚠️ 併せて＝**headless モックで詰めてから実機に出す手順（08-05「検証の手口」）が4件連続で当たった**。実機で作り直しになったものが無い。

---

## 2026-08-05

### 図鑑を「標本カード＋テープ」に（ChatGPT ラフからの採用と不採用）
ユーザーが ChatGPT に作らせた図鑑画面のラフを持ち込み、「ここまで装飾は要らないが参考になる」との評価。**装飾を剥がして構造だけを採用**した。
- **採用①＝標本番号（`F-001`／空きマスも `F-007 未発見`）**。いちばん効いた。`?` だけの空きマスは単なる余白だが、**番号が付くと「まだ埋まっていない席」になる**。しかも**①（長期の目標）の決定を待たずに入れられる**＝番号を「カタログの品番」でなく「**その人の図鑑の何番目か**」と読めば、分母が未定でも嘘にならない。
- **採用②＝台紙の上に「もう1枚の紙」を敷く**。これが「紙の上に写真が載っている」の正体。写真をページに直接置くと、白フチを付けてもアプリのサムネに見える。
- **採用③＝マスキングテープ**。CSS のみ（乳白の半透明グラデ＋`clip-path` で端をギザギザに）。**直線で切るとシールに見える**ので、ちぎった断面が要る。
- **採用④＝`第N章` と `◯種 発見`**。ただし**分母（`/36種`）は入れていない**＝出した瞬間に「フードは36種で完成」と宣言することになり、**①「器を有限にするか」を決めたことになる**ため。
- **不採用＝リング綴じ・ページのめくれ・唐草の飾り罫**。画像アセットか重い CSS が要り、「アート依存を作らない」方針と正面からぶつかる。①〜④だけで十分に「本」になる。
- **保留＝サブカテゴリ（フード/飲みもの）**。データに無く `identify` の responseSchema 変更が要る。①を決めてからの方が手戻りがない。
- **保留＝ちぎった縁**（`feTurbulence`+`feDisplacementMap` をカードの `::before` にだけ掛ければ写真と文字は歪まない、と実証済み）。カード1枚ごとにフィルタが走るので、図鑑が育ったときのモバイル描画を測ってから。
- **列数は 3 → 2**。カード化で1件の高さが増えるため。一覧性は落ちるが「1つ1つが標本」になる（ユーザー決定）。
- ⚠️ **判子に絵文字＋`grayscale`/`sepia` は使えない**＝「かすれたインクの印」でなく**灰色の塊**になる（レンダリングで確認）。丸い枠＋番号の頭文字で代替した。

### 検証の手口＝比較モックを headless Chrome で PNG 化する
UI の判断が要るときは、**実装前に比較用の単体 HTML を scratchpad に書いて `--force-device-scale-factor=2` でスクショ**し、現状と並べて見せる。図鑑の紙・標本カードはどちらもこれで一撃で決まった。実データが要らないので IndexedDB のシードも不要。

---

## 2026-08-04

### 紙の質感は「効く強さ」まで上げないと存在しない（台紙の作り直し）
実機で見て「全然紙の感じが出てない」と分かった。方向は正しかったが、**すべての signal を最も弱い設定にしていた**のが原因。測ると一目瞭然だった。
- **紙 `#FBF6EC` と body 背景のコントラスト比＝1.00〜1.04**。つまり紙が置かれていることすら視覚的に成立していなかった → **紙は周囲より濃くする**（`#F3E9D2`）。
- **ノイズ opacity 0.035 は画面上で不可視**。当初「0.03〜0.05 が上限、それ以上は汚れに見える」と書いていたが、並べて比べると**0.16 でようやく紙になる**。
- **実バグ＝ノイズの SVG に `width`/`height` が無かった**。SVG は固有サイズを持たないため背景として**器の全面に引き伸ばされ**、細かい粒が潰れていた。→ `width/height` ＋ `background-size` でタイル化（`stitchTiles='stitch'` で継ぎ目を消す）。
- **書体が変わっていなかった**。`font-zukan`＝M PLUS Rounded 1c に対し既定 sans が Zen Maru Gothic ＝**丸ゴシック→丸ゴシック**。→ **明朝**（Shippori Mincho）にして初めて「別の場所」になる。
- **空きマスの `？` のコントラスト比＝1.14**（可読の目安 4.5）＝見えていなかった。「集める先がある」が伝わらないので地を沈めて文字に濃さを持たせた。
- **角丸 16px ＋外へ広がる影は「カード」の記号**。角丸を落として影を内側（inset）へ入れると「紙の面」になる。
- **細い border だけの写真はアプリのサムネのまま**。→ **白フチ（マット）ごと 1 度未満だけ傾けて貼る**（傾きは id から決定的に＝乱数だと再レンダーで動いて安っぽい）。
- **紙の上にアプリのピル（丸い紫＋`shadow-pop`）を置かない**。プラスチックのボタンが紙に載って見立てが壊れる → インクの矩形に。
- ⚠️ **教訓＝質感は「入れたかどうか」でなく「知覚できる強さかどうか」**。cozy の遠慮でどれも最弱にすると、全部入れたのに何も出ない。

### API の回数制限（レート制限・日次上限）は入れない
実装したうえで**外した**。社内公開の段階のため。理由＝高コストな経路（召喚・合成＝1回約¥7）は**正しい形式の画像 data URL を POST しないと課金が発生しない**ので巡回ボットが偶然叩くことは実質なく、安い経路は単価が桁違いに低い。加えて**上限値そのものが「1人あたりどれだけ使わせるか」＝製品の方針を固定してしまう**ため、ユニットエコノミクスが決まるまで置く。
- **再検討のトリガー**＝①一般公開 ②社外テスターへの配布 ③請求が想定を超える ④STEP6（匿名認証）着手。
- 入れるときは per-IP のレート制限より**サーバ側の日次総量上限**（全ユーザー合計＝「1日いくらまで払うか」で決まる）を優先する。
- ※`api/` は無認証で公開されており、URL を知れば誰でも到達できる（アクセス制御は無い）＝**承知のうえでの状態**。

### 失敗をコレットのセリフで受ける（キャラ崩れフォールバック）
生のエラー（Gemini/Fish の文言・「合成に失敗しました」）を画面に出さず、`failureLines.ts` の文面で受ける。**カメラの判定失敗にだけセリフを置かない**＝写真はアルバムに残るので「図鑑に入らないだけ」は失敗ではない（風景を撮れば普通に起きる）。
- **コレットに「電波」「通信」を言わせない**＝persona の世界観では妖精界に機械が無く、彼女は通信の概念を知らない。技術的な事情は画面側の注記で出す（`CAMERA_HINT_TEXT` と同じ役割分担）。

### 図鑑を「標本の台紙」にした
アルバムとの差別化は切り抜きだけでなく**余白・縁・名前・並び**の4軸で作る、という整理から。紙の質感は**画像アセットを使わず CSS のみ**（`feTurbulence`・opacity 0.035）＝たからばこの背景と同じ「アート依存を作らない」方針。
- **見開き／ページめくりは実装しない**＝縦スクロールと喧嘩し、重く、片手で扱いづらい。「めくっている感じ」は章（カテゴリ）の区切りで出す。
- **罫線を px で固定しない**＝`html { font-size: clamp(...) }` で画面高に応じて全体がズームするので、行の実高が端末ごとに変わる。
- **座布団は入れていない**＝図鑑の画像は矩形の実写クロップで透過ではないため、いまは「紙に写真を貼った」見立て。**切り抜き（背景除去）を入れたら薄い楕円の座布団に差し替える**＝抜きの粗を紙の色に紛れさせる安全網になる。
- **未決＝空きマスが後退式**（埋まっている数＋3・`EMPTY_SLOT_LOOKAHEAD`）なので**実際には終わらない**。「終わる器」にしたければカテゴリごとの固定目標＋達成表示に切り替える。

---

## 2026-07-30

### 「出会いの三段」を実装したうえで外した（→二段）
音声の選択と登場の間に挟んでいた**ささやき**（「…ねえ、…だれか、そこにいるの…？」＝立ち姿なし・白いカードなし・文字の濃さを揺らす／ボタンを置かず数秒で自動遷移）と、1言目「……あっ！ ねえ、きみ。わたしのこと、見えてる？」（`surprised`）を撤去し、音声選択の直後に「はじめまして！」を置く二段に戻した。
- **理由＝実機で通しての評価「どうも最初のセリフがしっくりこない」**。⚠️ **これ以上の言語化はされていない**＝何が効かなかったのかは未分析。**この方向自体を否定したわけではなく、いいセリフ・展開を思いついたらまた足しうる**。
- 復元は commit `c58b346` を戻すだけ。
- 副次的な改善＝1枚目の読み上げが `primeAudio()` の直後に走るので**声の出だしがむしろ確実になった**。
- ⚠️ **教訓＝ユーザーのフィードバックを勝手に理屈づけて docs に固定しない**。当初この撤去に「前置き2枚で初対面が遠くなる」等の理由を*推測で*書いて指摘を受け、事実に戻した（`be087c5`）。

---

## 2026-07-28

### 最初の画面に世界観のナレーションを置かない
音声選択の画面に説明文も立ち絵も置かない。**話者のいないナレーターがコレットを紹介すると「誰の声？」になる**ため、世界の導入はコレット本人に一本化する。第一印象は時間帯背景＋質問だけ。
- 文体の register ＝ **プレーンな「〜しよう」体**（既存のシステム声「下の丸いボタンで撮ってみよう」が正解）。**体言止め・倒置・「そっと」「——」のようなポエミーな書き方は不採用**。
- 演出のカードに「つぎへ」ボタンを付けると"カードが1枚増えただけ"になって演出が死ぬ＝**自動遷移＋どこでもタップでスキップ**が正解（`TreasureOpening` と同じ扱い）。

### オンボの固定台詞はパートボイス（3b）で事前収録する
動的TTS は生成に1〜3秒かかるため、初対面の1言目や図鑑リビール・各コーチで**文字が先に出て声が遅れて追いつく**（＝間が抜ける）。台本は固定なので事前収録で解消する。

### iOS のホーム画面アプリは Safari と localStorage が別コンテナ
「なつき度/まほうパワーのアイコンを押しても何も起きない」＝バグではなく `anycollect.debug` が入っていない環境だった（しかもアドレスバーが無く `?debug=1` を打てない）。`DebugTap` は debug オフのとき**見た目が完全に同じ `<div>` を返す**ので「表示は同じなのにタップだけ死ぬ」ように見える。
- **判別法＝メニューに「初期化（全部消す・debug）」が出るかどうか**（同じ `debugTools()` を見るので必ず一致する）。
- **決定＝アプリ内トグルは足さない**。検証は debug 入りの Safari で行い、ホーム画面アプリは一般ユーザーの見え方の確認に使う。

---

## 2026-07-27

### まほうパワーの初期シードを撤去した
オンボ完了時に満タンを配る実装（`claimSeed`）を全撤去。**「まほうパワーが一発で100になる」＝"貯まる実感"が消える**ため。撮影+34／会話+20 で自然に貯め、満タンに到達したときに召喚コーチで案内する。
- 前提＝コーチを**永続の seen フラグ**方式にしたので、"満タン到達時"に出せる＝シードで場面を作る必要が無くなった。
- ⚠️ 07-26 に書いた「リビール閉じで claimSeed →満タン」はこの撤去で無効。

### オンボのコーチは phase の一本道でなく seen フラグで出す
`summonCoachSeen`/`treasureIntroSeen`/`chatCoachSeen` の3種を永続。**リロード/翌日でも各画面の初回で必ず一度出る**＝長いループが途中で切れても崩れない。
- **表示は store の seen からレンダー時に導出する**（effect 内で local setState しない）＝当初 `useState`＋effect で書いて `react-hooks/set-state-in-effect` を3件踏んだ。

### オンボの締めは召喚でなく図鑑リビール
メインコンテンツは「**きみとコレットの図鑑をつくる**」（実物を集める）と再確認したため。召喚は"図鑑を理解した次の発見"として後から現れる。
- **"空で放り出さない"は図鑑に既に1件あることで達成**（たからばこが空でも問題ない）。
- アイテムを指す**「子」呼びは全廃**（非人称化）。

### 「初期化」で喪失感が出た＝bonding は作り手にすら効く
検証用の全消去（`resetAllData`）を自分で使って喪失感が出た、という実証。**本番の削除（STEP6）は"お別れ"として重みを持たせる設計余地**がある（冷たい confirm で流さない／コレットがお別れを言う／消す前にエクスポートを促す）。

---

## 2026-07-25

### 正典ボイスは Fish Voice Design のオリジナル合成音声
テキストプロンプトから生成＝**実在人物に基づかない＝権利クリーン・クレジット表記も不要**。
- **つくよみちゃんコーパスのクローンは検証の末に不採用**（権利的にはクリーンだが要クレジット表記）。地色の元気さを直接指定できる Voice Design を採った。
- ⚠️ ROADMAP に「つくよみちゃんコーパスのクローン（要クレジット）」と書いたまま残っていた記述は 2026-08-04 に訂正済み。

---

## 2026-07-22

### なつき度・レベルの上限を撤廃（青天井）
旧仕様（3段・しきい値 30/100）は**会話17回ほどで最大に達し、以後どれだけ通っても何も起きない**＝「毎日開く理由」を作るための仕組みが初日で使い切れていた。
- **レベルの"意味"は後付けする前提**にし、いまは**後から復元できない素材の記録だけ先に始める**＝`firstAt`／`dayCount`（**連続でなく通算なので休んでも減らない**）／`bySource`。合計スコア1つに潰すとこれらは**二度と逆算できない**。
- 曲線＝**Lv2=30・Lv3=100 は据え置き**（既存データのレベルが動かない）→ Lv4 以降は 150 ごと。
- ⚠️ **口調 tier と立ち絵 tier は3段のまま**なので `toneTier()`（頭打ち版）を渡す。
- **効く出口は「口調」ではなく離散的で目に見える解禁**（呼び名／感情／たからばこの反応／声の段階解禁）＝口調は LLM が平均化するので体感差が出にくい（7/6 実機評価の教訓）。

### 会話履歴の切り詰めは「要約済みの分だけ」
未要約の会話を消すと**二度と記憶に入らない**ため。`consolidate` が**成功したときだけ**「どこまで要約済みか」を進める（従来は API を叩く前に進めていたので失敗分を取りこぼしていた）。位置は件数でなく**メッセージ id** で解決する（完了時に履歴が動いている可能性があるため）。絶対上限200件だけは安全弁として強制削除。

### 第一声は「会話が空のとき」でなく「前回から間が空いたとき」
リロードは静かに続きから始めたいため。30分以内は出さない／「久しぶり」は**日付が変わっただけでは名乗らない**（23時→翌1時は"さっきの続き"の感覚＝日跨ぎ＋3時間以上）。
- **第一声の生成中は、前回のセリフが残っていてもタイピングのドットに切り替える**＝「前のセリフが無言のまま突然差し替わる」と不気味なので、"何か言おうとしている"を必ず挟む。

### 背景の上に文字を置く＝「雲＋ハロー」の二段構え（3回作り直した）
半透過の白い箱＋フェザーマスクで「枠なし見え」を狙う方式は破綻する。①`backdrop-blur` は iOS Safari で backdrop-filter が mask にクリップされず四角が残る ②箱が `max-w-xs`＋`-inset-3` ＝ ほぼ画面幅ぴったり（370+24≒394px に対し iPhone は393px）なので**楕円マスクが透明になりきる前に画面の縁で切れる**。**真因は透明度でも blur でもなく"フェザーが画面内で完了していない"という寸法の問題**。
- 解＝**雲**（`inset-x-0` で箱を画面幅より必ず狭く保つ＋`ellipse 50% 50%` を明示した背景グラデ＝どの辺でも縁に達する前に alpha 0 まで落ちる）＋**ハロー**（文字ごとの `text-shadow` 4段重ね＝楕円が届かない四隅を受け持つ）。
- **雲を消してハローだけにすると「白い滲みが無くなった」と即座に気づかれた**＝白い雲は可読性だけでなく世界観の要素。片方だけにしない。

---

## 2026-07-21

### `filter: drop-shadow` は"光"に使わない（3箇所で踏んだ）
透過画像を浮かせようと `drop-shadow` を重ねると、**合成レイヤー上で影が要素の矩形に落ちて「四角い光」になる**（GPU 合成に乗った瞬間にアルファ形状でなくボックスで描かれる）。たからばこのアイテム（`0f863bf`）→ コレットの立ち絵（`67bd2af`）→ 入室演出の一枚絵、と同じ症状を繰り返した。
- **代替＝背景側に radial-gradient を敷いて後ろから光らせる**。絵そのものにフィルタをかけない。丸い要素に光を足すときは `box-shadow`（border-radius に沿うので矩形化しない）。

### 検証用ツールは `?debug=1` で既定オフ
実機（本番 Vercel）で URL に付けると有効化され localStorage に残る（`?debug=0` で解除）。**見た目は debug の ON/OFF で変わらない**（`button` ↔ `div` を差し替えるだけ）。これで「ローンチ前に検証ボタンを外す」宿題が消滅した。

### 漫符（漫画演出①）のプロトタイプは破棄・タグに固定
3週間シェルブしている間に `Sprite2DRenderer`／`tailwind.config.js` が動いて stash が当たらなくなったため掃除。**コードは git タグ `shelved/manpu-step11a` に固定**（`git show shelved/manpu-step11a` ＋ `^3` で SVG 本体まで読める）。再開は将来の漫画演出STEPでタグから当て直す。

---

## 2026-07-20

### アイテムの置き場を「妖精界」から「たからばこ」へ
作り込んだ風景シーン＝**背景アート素材への依存**が重かった。たからばこ＝4次元空間にアイテムが浮く収納ビューなら**背景をコード生成できる**（藍〜紫グラデ＋星屑＋底の光）。
- **妖精界は世界設定（正典）としては存続**＝コレットが住む隣の小さな世界で、たからばこはそこにある宝箱。`persona.md` は無改修。
- 永続フィールド名 `realmX/Y` は既存 IndexedDB データとの互換のため据え置き。

---

## 2026-07-19

### ホームの端末高さ吸収＝2段構え
viewport 高が可変（Safari アドレスバー有無・端末差・standalone）でも見た目を安定させる。試行錯誤の結論＝**余白の総量は決まっていて"どこに置くか"の問題**。全部上に寄せても全部下に寄せても、片寄せの帯は目立って嫌われる。
1. **UI 全体を画面高で一括ズーム**＝`html { font-size: clamp(16px, calc(100dvh/46), 20px) }`。Tailwind が rem 基準なので**ルート font-size を上げるだけで全要素が同比率で拡大**（引き伸ばしでなく拡大＝歪まない）。調整ノブ＝**除数46**（16pxになる高さ基準）と**上限20px**。下限16px＝iOS input の自動ズーム回避。
2. **残った余りは上下"均等"に割る**＝ヒーローを `flex-1 justify-center` で HUD とボタンの間に中央寄せ。下の薄い余白は「床＝部屋の奥行き」として自然に読める。
- 参考＝キャラ系アプリ（Pokémon GO・たまごっち系）は「キャラを大きく＋操作を下端固定＋余りは景色で吸収」。

### ホーム背景は時間帯フル4枚（窓だけ透過は不採用）
**フル4枚の方が室内の光まで時間帯に追従し、生成の一貫性も保てた**。木のうろの部屋（ChatGPT 生成）を `backgrounds/<背景ID>/` に置き、背景ID＝`tree-hollow`。これが「背景IDで差し替え可能なアセット」＝将来の背景・テーマ販売の土台になる。

---

## 2026-07-16

### ショップ＝機能をアイテムとして解禁する構想
狙い＝①世界観を崩さず機能を任意解禁 ②将来の大型機能を「新商品入荷」として導入する受け皿 ③コレットのおねだり＝背骨の演出装置 ④マネタイズの受け皿。**仕様の本体は spec §4.8**（構想・未実装）。
- **対象外＝コアループ（カメラ・会話・図鑑・召喚）＋アルバム**。アルバムは購入でなく**コレットからの贈り物**＝「記憶を人質にしない」原則の遵守。
- **おねだり規律**＝コレットが催促してよいのは**ゲーム内通貨で買える品のみ**。実課金品・サブスクは絶対に催促しない（キャラへの愛着を課金圧に使わない）。
- **通貨の実課金販売はしない**（ゲージ販売禁止と同根）。

---

## 2026-07-13

### 一問一答をやめる（会話の自然さ）
persona「会話の続け方」節（受けてから一言足す／質問は2回に1回以下・1返答に1つまで／話が一段落したらコレットから話題をふる／長さに揺らぎ／**例文は調子の見本でそのまま使わない**／一人称「わたし」はひらがな固定）＋ `buildSystemPrompt` の会話契約（「会話を前に進める」を明示）の二段で担保する。

### 「今日のおだい」という機能ラベルを撤回
コレットの"欲"を**達成リスト型のミッション/クエストUIにはしない**。cozy・streak 厳禁の哲学と正面から矛盾するため。**日々の会話の中で自然に漏らす**＝独立機能ではなく「良い会話＋接地」の創発物として実装する。

---

## 2026-07-12

### レイアウト再構成＝新IA（5タブ廃止）
おしゃべり/ずかん/アルバム/窯/置き場の5タブは狭い画面で詰まっていた。→ **ホーム＝会話が主役／入口＝図鑑・たからばこ・メニュー＋カメラ切替**。
- **召喚魔法＝図鑑エントリ→透過アイテム化を図鑑に移設**（旧「窯のアイテム化」）＝起点を図鑑に統一。
- **妖精の窯＝2アイテム合成に復活**（残置していた `synthesize` を配線）＝名実一致。**召喚と窯は別機能として両立**させる決着。
- 「元気」→**まほうパワー**に改称（召喚魔法に名を合わせた／`gaugeStore` の識別子 `GAUGE_*` は不変）。
- ゲームは妖精界からメニューへ移設。右下コレットは `WorkingScreen` 共通シェルに一本化。

---

## 2026-07-05

### レア度（rarity）を全廃
LLM の主観で真値がなく、cozy 路線とも噛み合わないため `Rarity` 型・`rarity.ts`・AI schema・表示・glow ごと削除（18ファイル・正味125行減）。収集の特別さは**初発見演出・発見回数・カテゴリ網羅**に一本化。
- 再導入するなら Rarity 型・`rarity.ts`・各 responseSchema・表示を戻す。

### 透過はクロマキー方式（Gemini のネイティブ透過は不採用）
Gemini は「透過」を**市松模様として描き込む**ため。サーバは単色マゼンタ背景（`ITEM_SOLID_BG`）で描き、クライアント canvas（`chromaKey.ts`）で背景色をアルファに抜く。
- 既知の割り切り＝**被写体にマゼンタ/濃ピンク/紫があると穴が空く**。

### 図鑑の説明は常体・体言止め（事典調）に固定
実機で「…場所だよ。」とコレット口調が漏れていたため、`buildIdentifySystemPrompt` の `subject.description` に文体の縛りと禁止（話し言葉・語りかけ・一人称・絵文字）を追加。**コレットの生セリフ（`comment`）とは分離**する。

### 「初回撮影が遅い」は仕様として受容
Vercel のコールドスタート由来（画像は1024px縮小済・identify は vision 1回）。コード変更なし。

### 危険対象は安全性を断定しない
きのこ/野草等で**食用可否・毒性・薬効を書かせない**ルールを identify プロンプトに追加。安全な日常物の図鑑説明は従来どおり。

---

## 2026-07-04

### カメラに Seek/iNaturalist 型の「図鑑」を足す
v2 で即アイテム化を外した結果、カメラが「反応＋ゲージ」だけになり**開く動機が薄れた**課題への解。撮った写真の**主役を判定→bbox でクロップ→種別に収集**。
- **二層の経済**＝判定（安価な vision）＋クロップ（無料）なので**収集は無制限**／高価なアイテム化だけまほうパワー配給。
- **図鑑が窯（＝召喚）の入口**に統合され、アルバムは思い出＋会話接地の独立ストリームとして残る（iNaturalist の観察ログ×ライフリスト構造）。
- `speciesKey` は固定タキソノミが無く LLM 生成キーなので**デデュープの揺らぎは割り切り**（将来 fuzzy 照合）。

---

## 2026-07-02

### v2 リデザイン＝コアを「撮影→即スキャン画像生成で収集」から「会話＋アルバム＋アイテムの置き場」へ
コスト・リテンションの両面から。カメラは「見せて反応をもらい、写真と図鑑に残す」場所になり、画像生成は召喚（1日1個）に絞られた。検討の全記録は開発計画 `~/.claude/plans/gemini-precious-riddle.md`。

### プライバシー方針を反転＝写真は保存する
旧「カメラの元写真を永続保存しない」をアルバム機能のため廃止。**既定はローカル端末のみ・クラウドは opt-in**、**エクスポート/削除を一級機能**にする、で責任を取る形に切り替えた。

---

## チューニングの実測値（日付をまたいで参照する数値）

### Fish TTS の感情タグは「日本語の自由文」で書く（2026-07-25 実証）
**英語のコアタグ（`[excited]`/`[sad]`）は日本語本文にはほぼ効かない**（出力のブレと区別できないレベル）。公式も「タグはスクリプトと同じ言語で」と推奨しており、**声優への演技指示のように書く**と明確に効く。
- 例：`[宝物を見つけた子どもみたいに、はしゃいで]` ／ `[泣きそうな声で、無理に平気なふりをして]`（後者は実際に泣き声が入る）。
- `s2.1-pro-free` でも効く（S2.1-Pro と機能同一）。**`latency:'low'` でも表現力は落ちない＝速さと表現は両取り**。
- 固定タグ（感情12種→同じ文）の限界＝`emotion:sad` の返事「…でも、今日は今日、明日は明日だよ！」を**励ましのセリフを暗く読む事故**になる。返事ごとの `voiceDirection` はこれを回避するために足した。

### 画像生成の実時間（2026-06-24 実測）
本番 Gemini の画像生成は **~7s**（平均7.3s／6.1〜8.5s）。「9〜11秒」に見えていたのは**ローカル dev／スマホ回線の上りアップロード律速**で、Vercel 到着後は速く一定。→ 実時間短縮（fal の高速モデル）でなく**体感改善UI**（`GeneratingOverlay`）で対応し、fal 組込は残置・保留。

### 物理ゲームは固定タイムステップ（2026-06-27 / 2026-07 タワー→フラッピー）
`Matter.Engine.update` に常に 1000/60ms を渡し、フレーム飛びは複数ステップで追いつく。**可変 dt だと積んだ山が実機のドラッグ中に不安定化して勝手に崩れる**（`positionIterations:8/velocityIterations:6` も併用）。フラッピーは最初からこれを適用。

### フラッピーの難易度（390×748・実エンジンを headless で決定的に駆動）
**難易度の本体は「隙間 ÷ 主役の当たり高さ」**。実機で「本家ほどの死にゲー感がない」と分かり、①隙間を本家より締め（`REF_GAP = 86`・本家100）②主役を大きくして当たりも大きくした（`birdSizeFor` 係数 0.14→0.17）。②は「アイテムが小さくて視認性が悪い」という実機FBも兼ねる。
- **周期を縮める方向は採らない**＝隙間中心の移動を吸収する余地まで削れて理不尽になる。
- **柱の周期は px でなくステップ数で固定**（84 step＝1.4秒）＝間隔を画面幅に比例させると PC の全画面で6秒に1本になり別ゲームになる。
- `GAP_PER_BIRD = 2.5` は**通れなくなるのを防ぐ安全下限**（横長画面用）で通常は効かない。ここを上げると `REF_GAP` の意味が消える。
- 実測＝隙間÷当たり高さ 3.12（本家4.17）／周期 1.40秒（本家1.40）／1はばたきの上昇は空の11%（本家11%）／**きつさ** 0.79（本家0.70・1.0で理論上ぎりぎり）。真ん中でホバリングするだけのAIは60秒で平均17.6本・5回中4回墜落。320×640 / 430×900 でも同値。

### 当たり判定は「見えている範囲」基準（フラッピー）
`buildSpritePiece` の**アルファ凸包の外接矩形 × 0.92**（中心ズレも補正）。透過アイテムは画像の外周に透明な余白を持つので、描画寸法をそのまま使うと**余白が多い絵は"何もない所で当たり"、少ない絵は"めり込んでも当たらない"**。

---

## 運用の教訓（日付なし・常に効く）

- **ユーザーのフィードバックを勝手に理屈づけて仕様書に固定しない**（2026-07-30 の項）。「しっくりこない」は「しっくりこない」のまま記録する。
- **仕様（値・ルール・方針）を変える前に必ず確認する**。実態追従の修正やバグ/セキュリティ修正は確認不要。
- **画像素材は WebP**（スプライト最大1024px／背景最大1536px）。png/jpg を足したら `npm run sprites:optimize` を実行してから commit（1枚~1MB→~120KB）。
- **Vercel の関数は nodenext(ESM)**＝`api/` の相対 import に `.js` 拡張子が必須（無いと実行時 `ERR_MODULE_NOT_FOUND`→500）。
- **⚠️ このリポジトリにはテストが1本もない**（vitest 未導入）。過去に純関数テストを合計76ケース書いて毎回捨てている。導入は保留中の判断。
